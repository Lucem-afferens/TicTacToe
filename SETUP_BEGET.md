# 🔧 Настройка на хостинге Beget

## 📋 Быстрая инструкция

### 1. Настройка токена бота

**✅ Автоматически через GitHub Secrets (рекомендуется)**

Токен автоматически устанавливается из GitHub Secrets при деплое:
1. Добавьте секрет `BOT_TOKEN` в настройках GitHub репозитория
2. При деплое автоматически создастся файл `config.local.php` с токеном
3. Файл не попадает в git и существует только на сервере

**Альтернативный способ: Через переменные окружения Beget**

Если хотите использовать переменные окружения Beget:
1. Зайдите в панель управления Beget
2. Найдите раздел "Переменные окружения" или "Environment Variables"
3. Добавьте переменную:
   - Имя: `BOT_TOKEN`
   - Значение: ваш токен бота

**Приоритет чтения токена:**
1. `config.local.php` (создается из GitHub Secrets при деплое) - **приоритет**
2. Переменная окружения `BOT_TOKEN` на сервере
3. Fallback (не используется в продакшн)

### 2. Структура директорий на Beget

Обычно файлы размещаются в:
```
/domains/develonik.ru/public_html/
```

Или если используется поддомен:
```
/domains/develonik.ru/public_html/subdomain/
```

### 3. Права доступа

После деплоя установите права:
```bash
chmod 755 data logs cache
chmod 644 data/*.json logs/*.log
chmod 600 config.local.php  # Защита конфига с токеном
```

### 4. Настройка webhook

После первого деплоя:
1. Откройте: `https://develonik.ru/setup/setup-webhook.php`
2. Скрипт автоматически настроит webhook

### 5. Проверка работы

1. Откройте бота: `https://t.me/TicTacToe_ru_bot`
2. Отправьте `/start`
3. Нажмите "🎮 Играть"
4. Проверьте работу игры

## 🔒 Безопасность

- ✅ Токен НЕ хранится в коде
- ✅ Токен читается из GitHub Secrets при деплое
- ✅ `config.local.php` создается только на сервере и НЕ попадает в git
- ✅ Файл защищен через `.htaccess` от прямого доступа
- ✅ `.env` файлы в `.gitignore`
- ✅ Конфигурационные файлы защищены через `.htaccess`

## 📝 Примечания

- Beget поддерживает PHP 7.4+ по умолчанию
- SSL сертификаты активируются через панель управления
- FTP доступ через `ftp.beget.com`
- GitHub Actions автоматически деплоит при пуше в main/master
- Токен автоматически устанавливается из GitHub Secrets

## 🔄 Процесс деплоя

1. **Пуш в main/master** → GitHub Actions запускается
2. **Создается `config.local.php`** с токеном из GitHub Secrets
3. **Файлы деплоятся на Beget** через FTP
4. **Токен доступен** через `config.local.php` на сервере
5. **Готово!** Бот работает без дополнительной настройки
