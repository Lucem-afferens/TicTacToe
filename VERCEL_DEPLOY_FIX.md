# 🔧 Исправление ошибки деплоя на Vercel

## Проблема: Deployment failed

Если деплой на Vercel не удается, выполните следующие шаги:

## ✅ Что было исправлено

1. **Упрощена конфигурация vercel.json**
   - Убран `outputDirectory` (может вызывать проблемы)
   - Упрощены маршруты (используются только rewrites)
   - Убраны сложные headers

2. **Проверьте следующее:**

### Шаг 1: Проверьте логи деплоя

1. Откройте Vercel Dashboard → Deployments
2. Выберите неудавшийся деплой
3. Откройте логи сборки (Build Logs)
4. Найдите ошибку (обычно выделена красным)

### Шаг 2: Проверьте структуру проекта

Убедитесь, что в репозитории есть:
- ✅ `dist/` директория с PHP файлами
- ✅ `api/` директория с setup-webhook.php
- ✅ `vercel.json` в корне проекта

### Шаг 3: Проверьте переменные окружения

1. Vercel Dashboard → Settings → Environment Variables
2. Убедитесь, что `BOT_TOKEN` установлен
3. Проверьте, что переменная добавлена для Production

### Шаг 4: Перезапустите деплой

1. Vercel Dashboard → Deployments
2. Найдите последний деплой
3. Нажмите "Redeploy"

## 🔍 Частые ошибки и решения

### Ошибка: "Cannot find module"
**Решение**: Убедитесь, что все файлы закоммичены в git

### Ошибка: "Build command failed"
**Решение**: Проверьте, что `buildCommand` в vercel.json корректен (или удалите его)

### Ошибка: "Function not found"
**Решение**: Проверьте пути в rewrites - они должны указывать на существующие файлы

### Ошибка: "Runtime not found"
**Решение**: Убедитесь, что `@vercel/php` указан в functions

## 📝 Минимальная рабочая конфигурация

Если проблемы продолжаются, используйте эту минимальную конфигурацию:

```json
{
  "version": 2,
  "functions": {
    "dist/**/*.php": {
      "runtime": "@vercel/php@2.0.0"
    },
    "api/**/*.php": {
      "runtime": "@vercel/php@2.0.0"
    }
  },
  "rewrites": [
    {
      "source": "/webhook.php",
      "destination": "/dist/webhook.php"
    },
    {
      "source": "/api/game.php",
      "destination": "/dist/api/game.php"
    },
    {
      "source": "/api/setup-webhook.php",
      "destination": "/api/setup-webhook.php"
    },
    {
      "source": "/web/:path*",
      "destination": "/dist/web/:path*"
    }
  ]
}
```

## 🆘 Если ничего не помогает

1. Проверьте документацию Vercel: https://vercel.com/docs
2. Обратитесь в поддержку Vercel с логами деплоя
3. Попробуйте создать новый проект в Vercel и подключить репозиторий заново

