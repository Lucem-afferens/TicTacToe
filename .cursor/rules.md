# Правила разработки проекта Telegram Bot Game «Крестики-Нолики»

## 🎯 Общие принципы

### Архитектура
- **Чёткое разделение**: bot / web / shared модули должны быть строго разделены
- **TypeScript**: Все файлы должны быть типизированы, избегать `any`
- **Модульность**: Каждый модуль отвечает за одну задачу
- **DRY**: Избегать дублирования кода, использовать общие утилиты из `shared/`

### Код-стиль
- **Именование**: 
  - Файлы: camelCase для TypeScript (game.ts, promo.ts)
  - Переменные/функции: camelCase
  - Константы: UPPER_SNAKE_CASE
  - Типы/Интерфейсы: PascalCase
- **Форматирование**: Использовать Prettier с настройками проекта
- **Комментарии**: JSDoc для публичных функций, краткие комментарии для сложной логики

### Безопасность
- **Токены**: Никогда не коммитить токены в код, использовать `.env` файлы
- **Валидация**: Все пользовательские данные должны валидироваться
- **Telegram Web App**: Проверять `initData` от Telegram перед обработкой

## 🤖 Telegram Bot специфичные правила

### Структура бота
- Использовать `grammy` для TypeScript
- Обработчики команд должны быть в отдельных функциях
- Сообщения выносить в `messages.ts` для локализации
- Использовать Web App кнопки для открытия игры

### Обработка ошибок
- Все async функции должны иметь try-catch
- Логировать ошибки с контекстом (userId, command)
- Пользователю показывать дружелюбные сообщения об ошибках

## 🎮 Game Logic правила

### Логика игры
- Игрок всегда ходит первым (X)
- Бот играет O
- Проверка победы должна быть оптимизирована
- AI должен быть не идеальным (60% случайность, 30% блокировка, 10% выигрыш)

### Промокоды
- Генерировать только при победе игрока
- Формат: 5 цифр (10000-99999)
- Уникальность не требуется (каждая победа = новый код)
- Отправлять в Web UI и в Telegram бот

## 🌐 Web App правила

### Telegram Web App SDK
- Всегда инициализировать SDK в начале загрузки
- Использовать `window.Telegram.WebApp` для взаимодействия
- Отправлять данные в бот через `sendData()`
- Использовать `expand()` для полноэкранного режима

### Производительность
- Минимальный размер бандла
- Ленивая загрузка ресурсов
- Оптимизация изображений (WebP, сжатие)
- Кэширование статических ресурсов

### Адаптивность
- Mobile-first подход
- Тестировать на разных размерах экранов Telegram
- Использовать CSS Grid/Flexbox для адаптивности
- Учитывать безопасные зоны (notch, status bar)

### UX/UI
- Плавные анимации (transition, transform)
- Обратная связь на каждое действие пользователя
- Состояния загрузки для async операций
- Чёткие визуальные индикаторы состояния игры

## 📁 Структура файлов

### Импорты
- Абсолютные импорты через алиасы (@shared, @bot, @web)
- Группировать импорты: внешние → внутренние → типы
- Не использовать циклические зависимости

### Экспорты
- Named exports предпочтительнее default exports
- Экспортировать типы отдельно от реализации
- Использовать barrel exports (index.ts) для модулей

## ⚠️ Важные ограничения

- **Не изменять CSS стили** без явного запроса пользователя
- **Удалять устаревшие файлы** при рефакторинге
- **Не коммитить токены** и секретные данные
- **Следовать дизайн-системе** из `DESIGN_SYSTEM.md`
