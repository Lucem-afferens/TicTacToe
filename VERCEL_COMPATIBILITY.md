# 🔍 Проверка совместимости с Vercel

## ✅ Что работает на Vercel

### Node.js Serverless Functions (api/)
- ✅ `api/webhook.js` - обработчик webhook от Telegram
- ✅ `api/setup-webhook.js` - настройка webhook
- ✅ `api/check-webhook.js` - проверка настроек
- ✅ `api/game.js` - API для игры

### Статические файлы
- ✅ `dist/web/game.html` - главная страница игры
- ✅ `dist/web/assets/css/*` - стили (доступны через rewrite)
- ✅ `dist/web/assets/js/*` - JavaScript (доступны через rewrite)
- ✅ `public/web/assets/images/*` - изображения (доступны напрямую)

### Конфигурация
- ✅ `vercel.json` - конфигурация маршрутизации
- ✅ `package.json` - зависимости и настройки Node.js

## ❌ Что НЕ работает на Vercel

### PHP файлы
Все PHP файлы в `dist/` не выполняются на Vercel, так как нет PHP runtime:
- ❌ `dist/web/game.php` - заменен на `game.html`
- ❌ `dist/api/game.php` - заменен на `api/game.js`
- ❌ `dist/webhook.php` - заменен на `api/webhook.js`
- ❌ `dist/setup/setup-webhook.php` - заменен на `api/setup-webhook.js`
- ❌ Все остальные PHP файлы в `dist/` - используются только для справки

**Примечание:** PHP файлы остаются в проекте для справки, но не используются на Vercel.

## 📁 Структура для Vercel

```
/
├── api/                    # Node.js serverless functions
│   ├── webhook.js         ✅ Работает
│   ├── setup-webhook.js   ✅ Работает
│   ├── check-webhook.js   ✅ Работает
│   └── game.js            ✅ Работает
├── dist/                   # Исходные файлы
│   └── web/               # Веб-приложение
│       ├── game.html      ✅ Работает (через rewrite)
│       └── assets/        ✅ Работает (через rewrite)
├── public/                 # Статические файлы
│   └── web/
│       └── assets/
│           └── images/    ✅ Работает (доступны напрямую)
├── vercel.json            ✅ Конфигурация
└── package.json           ✅ Зависимости
```

## 🔧 Настройка статических файлов

### Изображения
Изображения доступны по двум путям:
1. `/web/assets/images/X.png` - через rewrite из `dist/web/assets/images/`
2. `/web/assets/images/X.png` - напрямую из `public/web/assets/images/` (приоритет)

### CSS и JS
CSS и JS файлы доступны через rewrite:
- `/web/assets/css/*` → `dist/web/assets/css/*`
- `/web/assets/js/*` → `dist/web/assets/js/*`

## 🚀 Рекомендации

1. **Используйте SVG вместо PNG** - встроенные SVG всегда работают
2. **Проверяйте пути** - используйте абсолютные пути от корня
3. **Кэширование** - статические файлы кэшируются автоматически
4. **PHP файлы** - можно удалить из `dist/`, если не нужны для справки

