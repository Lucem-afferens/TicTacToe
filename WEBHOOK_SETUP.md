# 🔗 Настройка Telegram Webhook

## 📋 Обзор

Бот поддерживает два режима работы:
- **Polling** (для разработки) - бот сам запрашивает обновления у Telegram
- **Webhook** (для продакшена) - Telegram отправляет обновления на ваш сервер

## 🚀 Быстрый старт

### 1. Автоматическая настройка webhook (РЕКОМЕНДУЕТСЯ)

После деплоя бота на сервер:

1. **Запустите webhook сервер:**
   ```bash
   cd /path/to/bot  # или cd public_html/bot
   npm install --production
   npm run build:bot
   npm run start:webhook
   ```

2. **Откройте в браузере:**
   ```
   https://develonik.ru/setup-webhook
   ```
   
   Webhook автоматически настроится! Вы увидите красивую страницу с подтверждением.

### 2. Ручная настройка webhook (альтернатива)

Если предпочитаете командную строку:

```bash
# Перейдите в директорию бота
cd /path/to/bot  # или cd public_html/bot

# Установите зависимости (если ещё не установлены)
npm install --production

# Соберите проект (если ещё не собран)
npm run build:bot

# Настройте webhook через скрипт
npm run setup-webhook
```

### 2. Переменные окружения

Убедитесь, что в `.env` файле установлены:

```env
BOT_TOKEN=your_bot_token_here
BOT_USERNAME=TicTacToe_ru_bot
WEB_APP_URL=https://develonik.ru/
WEBHOOK_URL=https://develonik.ru/webhook  # URL для webhook
WEBHOOK_PORT=3001  # Порт для webhook сервера (опционально)
USE_WEBHOOK=true  # Включить режим webhook
NODE_ENV=production
```

**Важно:**
- `WEBHOOK_URL` должен быть доступен по HTTPS
- `WEBHOOK_URL` должен содержать путь `/webhook`
- Если `WEBHOOK_URL` не указан, используется `${WEB_APP_URL}/webhook`

### 3. Запуск webhook сервера

```bash
# Запуск через npm script
npm run start:webhook

# Или напрямую через node
USE_WEBHOOK=true WEBHOOK_PORT=3001 node dist/bot/webhook-server.js
```

### 4. Проверка работы

1. Отправьте `/start` боту в Telegram
2. Проверьте логи сервера - должны появиться обновления
3. Нажмите кнопку "🎮 Играть" и проверьте работу игры

## 📝 Команды бота

### Доступные команды:

- `/start` - Приветствие и начало игры
- `/help` - Справка по игре и командам
- `/game` - Быстрый старт игры

### Сообщения бота:

- **Приветствие** - приветственное сообщение с правилами игры
- **Победа** - сообщение с промокодом и кнопкой "Сыграть ещё"
- **Проигрыш** - ободряющее сообщение с кнопкой "Попробовать ещё раз"
- **Ничья** - сообщение с кнопкой "Сыграть ещё раз"

## 🔧 Настройка на Beget

### Вариант 1: Webhook на том же домене (рекомендуется)

Если бот работает на том же домене что и Web App:

1. Убедитесь что `WEB_APP_URL=https://develonik.ru/`
2. Webhook автоматически будет установлен на `https://develonik.ru/webhook`
3. Настройте проксирование в веб-сервере (nginx/apache) на порт `3001`

**Пример конфигурации nginx:**

```nginx
location /webhook {
    proxy_pass http://localhost:3001/webhook;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
}
```

### Вариант 2: Webhook на отдельном поддомене

Если хотите использовать отдельный поддомен:

1. Создайте поддомен (например, `bot.develonik.ru`)
2. Установите `WEBHOOK_URL=https://bot.develonik.ru/webhook`
3. Настройте проксирование на порт `3001`

## 🛠️ Управление webhook

### Проверка статуса webhook

```bash
# Через скрипт setup-webhook
npm run setup-webhook
```

Скрипт покажет:
- Текущий URL webhook
- Количество ожидающих обновлений
- Последнюю ошибку (если есть)

### Удаление webhook

```bash
# Временно переключиться на polling
node -e "import('grammy').then(({Bot})=>new Bot(process.env.BOT_TOKEN).api.deleteWebhook({drop_pending_updates:true}))"
```

### Обновление webhook

Просто запустите `npm run setup-webhook` снова - старый webhook будет заменён новым.

## 🔍 Troubleshooting

### Проблема: Webhook не работает

1. **Проверьте URL:**
   ```bash
   curl https://develonik.ru/webhook
   ```
   Должен вернуть 404 или JSON ответ (не ошибку подключения)

2. **Проверьте логи сервера:**
   ```bash
   pm2 logs tictactoe-bot
   # или
   tail -f bot.log
   ```

3. **Проверьте статус webhook:**
   ```bash
   npm run setup-webhook
   ```

### Проблема: "Webhook URL должен начинаться с https://"

Убедитесь что:
- `WEB_APP_URL` начинается с `https://`
- `WEBHOOK_URL` (если указан) начинается с `https://`

### Проблема: Обновления не приходят

1. Проверьте что webhook сервер запущен
2. Проверьте что порт `3001` доступен
3. Проверьте что проксирование настроено правильно
4. Проверьте логи на наличие ошибок

### Проблема: "Could not create file" при деплое

Это ошибка FTP, не связанная с webhook. Проверьте:
- Правильность путей в GitHub Secrets
- Права доступа на FTP сервере

## 📚 Дополнительная информация

- [Telegram Bot API - Webhooks](https://core.telegram.org/bots/api#setwebhook)
- [Grammy Documentation](https://grammy.dev/guide/deployment-types.html#webhooks)

