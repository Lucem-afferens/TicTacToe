# 🚀 Инструкция по деплою на Beget

## 📋 Настройка GitHub Secrets

1. Откройте: https://github.com/Lucem-afferens/TicTacToe/settings/secrets/actions
2. Нажмите **"New repository secret"**
3. Добавьте следующие секреты:

### Обязательные секреты:

| Имя | Значение | Пример |
|-----|----------|--------|
| `FTP_HOST` | Хост FTP сервера Beget | `develonik.ru` |
| `FTP_USERNAME` | Ваш логин FTP Beget | `u1234567` (ваш логин) |
| `FTP_PASSWORD` | Ваш пароль FTP Beget | `ваш_пароль` |
| `FTP_WEB_PATH` | Путь на сервере для Web App | `/public_html` или `/` |

### Как узнать данные для Beget:

1. Войдите в панель управления Beget
2. Перейдите в **"FTP"** → **"Доступы"**
3. Найдите или создайте FTP пользователя
4. Скопируйте:
   - **Хост**: обычно `develonik.ru` или IP адрес
   - **Логин**: ваш FTP логин
   - **Пароль**: ваш FTP пароль
   - **Путь**: обычно `/public_html` для основного домена

## 🔧 Настройка workflow

Workflow уже настроен правильно:
- ✅ Путь сборки: `dist/web/`
- ✅ Путь деплоя: `./dist/web/`
- ✅ Исключения настроены

## 🚀 Запуск деплоя

### Автоматический деплой:
- При каждом push в ветку `main` → автоматический деплой

### Ручной деплой:
1. Откройте: https://github.com/Lucem-afferens/TicTacToe/actions
2. Выберите workflow **"Deploy TicTacToe Bot & Web App"**
3. Нажмите **"Run workflow"**
4. Выберите ветку `main`
5. Нажмите **"Run workflow"**

## ✅ Проверка деплоя

После успешного деплоя:

1. Проверьте файлы на сервере:
   - Откройте FTP клиент
   - Подключитесь к Beget
   - Проверьте папку `/public_html` (или указанный путь)
   - Должны быть файлы: `index.html`, `assets/`

2. Проверьте доступность:
   - Откройте: https://develonik.ru/
   - Должна открыться игра

3. Проверьте логи GitHub Actions:
   - Откройте: https://github.com/Lucem-afferens/TicTacToe/actions
   - Выберите последний запуск
   - Проверьте логи на ошибки

## 🐛 Troubleshooting

### Проблема: "Failed to connect to FTP server"

**Решение:**
- Проверьте `FTP_HOST` (должен быть без `ftp://` и без порта)
- Проверьте `FTP_USERNAME` и `FTP_PASSWORD`
- Убедитесь, что FTP доступ разрешён в панели Beget

### Проблема: "Permission denied"

**Решение:**
- Проверьте `FTP_WEB_PATH` (должен быть правильный путь)
- Убедитесь, что у FTP пользователя есть права на запись
- Попробуйте путь `/public_html` или `/`

### Проблема: "No files to deploy"

**Решение:**
- Проверьте, что сборка прошла успешно
- Проверьте логи шага "Build Web App"
- Убедитесь, что `dist/web/` содержит файлы

### Проблема: Файлы загружены, но сайт не работает

**Решение:**
- Проверьте, что `index.html` в корне сайта
- Проверьте пути к assets (должны быть относительные)
- Проверьте консоль браузера на ошибки
- Убедитесь, что сервер поддерживает HTTPS (обязательно для Telegram Web App)

## 📝 Структура файлов на сервере

После деплоя на Beget структура должна быть:

```
/public_html/  (или указанный FTP_WEB_PATH)
├── index.html
└── assets/
    ├── main-xxxxx.css
    └── main-xxxxx.js
```

## 🔗 Полезные ссылки

- GitHub Actions: https://github.com/Lucem-afferens/TicTacToe/actions
- GitHub Secrets: https://github.com/Lucem-afferens/TicTacToe/settings/secrets/actions
- Beget панель: https://beget.com/ru/panel

