# 🚀 Автоматический запуск бота

## 📋 Обзор

После деплоя бота на сервер, всё можно запустить одной командой!

## 🎯 Быстрый старт

### После деплоя на сервер:

1. **Подключитесь к серверу через SSH:**
   ```bash
   ssh ваш_логин@develonik.ru
   ```

2. **Перейдите в директорию бота:**
   ```bash
   cd /path/to/bot  # или cd public_html/bot
   ```

3. **Запустите автоматический скрипт:**
   ```bash
   bash install-and-start.sh
   ```

**Всё!** Скрипт автоматически:
- ✅ Установит зависимости (`npm install --production`)
- ✅ Соберёт проект (`npm run build:bot`)
- ✅ Запустит webhook сервер (`npm run start:webhook`)

## 📝 Что делают скрипты

### `install-and-start.sh`
Полный скрипт установки и запуска:
- Проверяет наличие Node.js и npm
- Проверяет наличие .env файла
- Устанавливает зависимости
- Собирает проект
- Запускает бота через `start.sh`

### `start.sh`
Скрипт запуска бота:
- Проверяет наличие .env
- Устанавливает зависимости (если нужно)
- Собирает проект (если нужно)
- Запускает через PM2 (если установлен) или напрямую

## 🔄 Перезапуск бота

### Если используете PM2:
```bash
pm2 restart tictactoe-webhook
# или
pm2 reload tictactoe-webhook
```

### Если запущен напрямую:
```bash
# Остановка
kill $(cat webhook.pid)

# Запуск заново
bash start.sh
```

## 📊 Проверка статуса

### PM2:
```bash
pm2 status
pm2 logs tictactoe-webhook
```

### Прямой запуск:
```bash
tail -f webhook.log
ps aux | grep webhook
```

## 🔧 Настройка автозапуска

### Через PM2 (рекомендуется):
```bash
pm2 startup
pm2 save
```

### Через systemd:
Создайте файл `/etc/systemd/system/tictactoe-webhook.service`:
```ini
[Unit]
Description=TicTacToe Telegram Bot Webhook
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/path/to/bot
Environment="NODE_ENV=production"
Environment="USE_WEBHOOK=true"
ExecStart=/usr/bin/node dist/bot/webhook-server.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable tictactoe-webhook
sudo systemctl start tictactoe-webhook
```

## 🎉 После запуска

1. **Откройте в браузере:**
   ```
   https://develonik.ru/setup-webhook
   ```

2. **Webhook автоматически настроится!**

3. **Проверьте бота:**
   - Отправьте `/start` боту в Telegram
   - Нажмите кнопку "🎮 Играть"

## 🔍 Troubleshooting

### Проблема: "Node.js не найден"
```bash
# Установите Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Проблема: "npm не найден"
```bash
# npm устанавливается вместе с Node.js
# Если всё ещё не работает, переустановите Node.js
```

### Проблема: "PM2 не найден"
```bash
# Установите PM2 глобально
npm install -g pm2
```

### Проблема: "Порт 3001 занят"
```bash
# Измените порт в .env
WEBHOOK_PORT=3002
```

### Проблема: Бот не отвечает
1. Проверьте логи: `pm2 logs` или `tail -f webhook.log`
2. Проверьте webhook: откройте `https://develonik.ru/setup-webhook`
3. Проверьте что сервер запущен: `pm2 status` или `ps aux | grep webhook`

