# 🚀 Инструкция по развертыванию игры "Крестики-Нолики"

## ✅ Что уже настроено

- ✅ PHP 7.4+
- ✅ Структура проекта
- ✅ Telegram Bot API
- ✅ JSON файлы для хранения данных
- ✅ Логирование
- ✅ GitHub Actions для автоматического деплоя
- ✅ Безопасное хранение токенов через переменные окружения

## 🌐 Информация о продакшн

- **Домен**: develonik.ru
- **Бот**: @TicTacToe_ru_bot
- **Хостинг**: Beget
- **Деплой**: Автоматический через GitHub Actions

## 📁 Шаг 1: Настройка GitHub Secrets

В настройках репозитория GitHub добавьте следующие секреты:

1. **BOT_TOKEN** - токен Telegram бота (получите у @BotFather)
2. **FTP_HOST** - FTP хост Beget (обычно `ftp.beget.com`)
3. **FTP_USERNAME** - ваш FTP логин
4. **FTP_PASSWORD** - ваш FTP пароль
5. **FTP_PATH** - путь на сервере (например `/domains/develonik.ru/public_html`)

### Как добавить секреты:
1. Откройте репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. New repository secret
4. Добавьте каждый секрет отдельно

## 🔧 Шаг 2: Настройка на сервере Beget

### 2.1 Настройка токена бота

**✅ Автоматически через GitHub Secrets (рекомендуется)**

Токен автоматически устанавливается из GitHub Secrets при деплое:
1. Добавьте секрет `BOT_TOKEN` в настройках GitHub репозитория (Settings → Secrets → Actions)
2. При деплое автоматически создастся файл `config.local.php` с токеном
3. Файл не попадает в git и существует только на сервере
4. **Никаких дополнительных действий на Beget не требуется!**

**Альтернативный способ: Через переменные окружения Beget**

Если хотите использовать переменные окружения Beget:
- Зайдите в панель управления хостингом
- Найдите раздел "Переменные окружения"
- Добавьте `BOT_TOKEN` со значением токена

**Приоритет чтения токена:**
1. `config.local.php` (из GitHub Secrets) - **приоритет**
2. Переменная окружения `BOT_TOKEN` на сервере
3. Fallback (не используется в продакшн)

### 2.2 Настройка прав доступа

После первого деплоя проверьте права доступа:
```bash
chmod 755 data logs cache
chmod 644 data/*.json logs/*.log
```

## 🔒 Шаг 3: Настройка SSL

Beget предоставляет бесплатные SSL сертификаты Let's Encrypt:

1. Зайдите в панель управления Beget
2. Найдите раздел "SSL сертификаты"
3. Включите SSL для домена `develonik.ru`
4. Дождитесь активации (обычно несколько минут)

## 🤖 Шаг 4: Настройка Telegram бота

### 4.1 Автоматическая настройка webhook

После деплоя откройте в браузере:
```
https://develonik.ru/setup/setup-webhook.php
```

Скрипт автоматически:
- Удалит старый webhook
- Установит новый webhook
- Настроит команды бота
- Проверит работу бота

### 4.2 Ручная настройка (альтернатива)

Если автоматическая настройка не сработала:
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url":"https://develonik.ru/webhook.php"}'
```
Замените `ВАШ_ТОКЕН` на реальный токен бота.

## 📱 Шаг 5: Тестирование

### 5.1 Проверка webhook
```bash
curl https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo
```
Замените `ВАШ_ТОКЕН` на реальный токен бота.

### 5.2 Проверка бота
1. Найдите бота @TicTacToe_ru_bot в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🎮 Играть"
4. Проверьте, что игра открывается

### 5.3 Проверка игры
1. Сыграйте полную игру
2. Проверьте генерацию промокода при победе
3. Проверьте отправку промокода в бот
4. Проверьте сохранение данных в JSON

## 🔄 Шаг 6: Автоматический деплой

Проект настроен на автоматический деплой через GitHub Actions:

1. **При пуше в main/master ветку**:
   - Автоматически запускается workflow
   - Файлы деплоятся на Beget через FTP
   - Сохраняются только файлы из `dist/`

2. **Что НЕ деплоится**:
   - `.env` файлы
   - `node_modules`
   - Локальные конфигурации
   - Документация

3. **После деплоя**:
   - Проверьте работу бота
   - При необходимости перезапустите webhook

## 📊 Шаг 7: Мониторинг

### 7.1 Логи
Логи сохраняются в папке `logs/`:
- `app.log` - все логи приложения

Проверка логов через FTP или SSH:
```bash
tail -f logs/app.log
```

### 7.2 Проверка данных
Проверьте файлы в `data/`:
- `games.json` - история игр
- `promo-codes.json` - выданные промокоды
- `statistics.json` - статистика

### 7.3 Мониторинг через Beget
- Используйте панель управления Beget для мониторинга
- Проверяйте статистику использования ресурсов
- Следите за ошибками в логах сервера

## 🚨 Шаг 8: Безопасность

### 8.1 Обязательные меры
- ✅ Токен бота хранится в переменных окружения (НЕ в коде)
- ✅ `.env` файлы в `.gitignore`
- ✅ Защита конфигурационных файлов через `.htaccess`
- ✅ Rate limiting включен
- ✅ Валидация всех входных данных

### 8.2 Дополнительные меры
- Регулярно проверяйте логи на подозрительную активность
- Делайте резервные копии JSON файлов
- Обновляйте PHP при возможности
- Мониторьте использование ресурсов

## 🔧 Шаг 9: Особенности Beget

### 9.1 Структура директорий
На Beget файлы обычно размещаются в:
```
/domains/develonik.ru/public_html/
```

### 9.2 PHP настройки
Beget поддерживает PHP 7.4+ по умолчанию. Проверьте версию в панели управления.

### 9.3 FTP доступ
- Хост: `ftp.beget.com`
- Порт: `21`
- Используйте данные из панели управления

### 9.4 SSL сертификаты
Beget предоставляет бесплатные SSL через Let's Encrypt. Активируйте в панели управления.

## 📞 Шаг 10: Поддержка

### 10.1 Полезные команды
```bash
# Проверка статуса сервера
curl -I https://develonik.ru/

# Проверка SSL
openssl s_client -connect develonik.ru:443

# Проверка webhook
curl https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo
```
Замените `ВАШ_ТОКЕН` на реальный токен бота.

### 10.2 Контакты для поддержки
- **Telegram Bot API**: @BotSupport
- **Beget поддержка**: через панель управления хостингом

## 🎉 Готово!

Ваше приложение "Крестики-Нолики" готово к работе!

**Ссылки:**
- 🌐 Веб-приложение: `https://develonik.ru/web/game.php`
- 🤖 Telegram бот: `https://t.me/TicTacToe_ru_bot`
- 🔧 Webhook: `https://develonik.ru/webhook.php`
- ⚙️ Настройка webhook: `https://develonik.ru/setup/setup-webhook.php`

**Важные данные:**
- Telegram Bot Token: хранится в секретах GitHub и переменных окружения сервера
- Домен: develonik.ru
- Права доступа: 755 для папок, 644 для файлов

## 🔄 Процесс обновления

1. Внесите изменения в код
2. Закоммитьте и запушьте в main/master
3. GitHub Actions автоматически задеплоит изменения
4. Проверьте работу бота

**Примечание**: При обновлении данные пользователей (JSON файлы) сохраняются, так как они не перезаписываются при деплое.
