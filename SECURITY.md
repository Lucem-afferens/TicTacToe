# 🔒 Руководство по безопасности проекта

> ⚠️ **КРИТИЧЕСКИ ВАЖНО**: Следуйте этим правилам для защиты вашего проекта и данных пользователей.

---

## 🚨 Что НИКОГДА не должно попадать в репозиторий

### 1. Токены и секреты

❌ **НЕ КОММИТЬТЕ**:
- Токены Telegram ботов
- API ключи
- Пароли
- Приватные ключи SSH
- Секретные ключи шифрования

✅ **ВМЕСТО ЭТОГО**:
- Используйте переменные окружения (`.env` файлы)
- Используйте GitHub Secrets для CI/CD
- Используйте `.env.example` для документации

### 2. Файлы с чувствительными данными

❌ **НЕ КОММИТЬТЕ**:
- `.env` и все его варианты (`.env.local`, `.env.production`, и т.д.)
- Файлы конфигурации с реальными данными
- Файлы с токенами в названии (`*token*`, `*secret*`, `*password*`)

✅ **ПРОВЕРЬТЕ**:
- `.gitignore` содержит все необходимые правила
- Перед коммитом проверьте: `git status` и `git diff`

---

## ✅ Что делать, если токен уже в репозитории

### Шаг 1: Немедленно отозвать токен

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/revoke` или `/mybots`
3. Выберите вашего бота
4. Выберите "API Token" → "Revoke current token"
5. Получите новый токен

### Шаг 2: Удалить токен из истории Git

⚠️ **ВАЖНО**: Если репозиторий уже публичный, токен нужно удалить из истории Git.

#### Вариант A: Если репозиторий только что создан (нет важных коммитов)

```bash
# Удалить последний коммит (если токен только там)
git reset --soft HEAD~1

# Или удалить все коммиты и начать заново
git checkout --orphan new-main
git add .
git commit -m "Initial commit (без токенов)"
git branch -D main
git branch -m main
git push -f origin main
```

#### Вариант B: Если уже есть история коммитов

Используйте `git-filter-repo` или `BFG Repo-Cleaner`:

```bash
# Установить git-filter-repo
pip install git-filter-repo

# Удалить токен из всей истории
git filter-repo --invert-paths --path примерный-запрос.md
git filter-repo --invert-paths --path ПРОЕКТ-ЗАПРОС.md

# Или использовать BFG (быстрее для больших репозиториев)
# java -jar bfg.jar --replace-text tokens.txt
```

#### Вариант C: Использовать GitHub API (если репозиторий на GitHub)

GitHub может помочь удалить чувствительные данные через их API или поддержку.

### Шаг 3: Обновить файлы без токенов

1. Удалите токены из всех файлов
2. Используйте `.env.example` для примеров
3. Добавьте `.env` в `.gitignore`

### Шаг 4: Принудительно обновить удалённый репозиторий

```bash
# После очистки истории
git push --force origin main
```

⚠️ **ВНИМАНИЕ**: `--force` перезапишет историю. Убедитесь, что все участники проекта синхронизированы!

---

## 🛡️ Правильная настройка безопасности

### 1. Настройка `.gitignore`

Убедитесь, что `.gitignore` содержит:

```gitignore
# Environment files
.env
.env.*
!.env.example

# Secrets
**/*token*
**/*secret*
**/*password*
**/*key*
```

### 2. Создание `.env.example`

Создайте `.env.example` с примерами (без реальных значений):

```env
BOT_TOKEN=your_bot_token_here
BOT_USERNAME=TicTacToe_ru_bot
WEB_APP_URL=https://your-domain.com
```

### 3. Использование переменных окружения

В коде используйте:

```typescript
// ✅ ПРАВИЛЬНО
const token = process.env.BOT_TOKEN;
if (!token) {
  throw new Error('BOT_TOKEN не установлен');
}

// ❌ НЕПРАВИЛЬНО
const token = '8511369237:AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o';
```

### 4. Настройка GitHub Secrets (для CI/CD)

1. Откройте репозиторий на GitHub
2. Перейдите в Settings → Secrets and variables → Actions
3. Добавьте секреты:
   - `BOT_TOKEN` - токен бота
   - `FTP_HOST` - хост FTP сервера
   - `FTP_USERNAME` - имя пользователя FTP
   - `FTP_PASSWORD` - пароль FTP
   - И другие секреты

В workflow используйте:

```yaml
env:
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
```

---

## 🔍 Проверка перед коммитом

### Чеклист безопасности

Перед каждым коммитом проверьте:

- [ ] Нет ли токенов в изменённых файлах?
- [ ] Нет ли паролей в коде?
- [ ] Все ли `.env` файлы в `.gitignore`?
- [ ] Используются ли переменные окружения для секретов?
- [ ] Нет ли секретов в комментариях?

### Команды для проверки

```bash
# Проверить, что .env не отслеживается
git ls-files | grep -E '\.env$|\.env\.'

# Поиск токенов в коде (перед коммитом)
grep -r "8511369237\|AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o" . --exclude-dir=node_modules

# Проверить, что будет закоммичено
git diff --cached

# Проверить .gitignore
git check-ignore -v .env
```

---

## 📋 Рекомендации для публичного репозитория

### 1. Используйте `.env.example`

Создайте `.env.example` с примерами (без реальных значений) для документации.

### 2. Документируйте требования

В `README.md` укажите:

```markdown
## Настройка

1. Скопируйте `.env.example` в `.env`
2. Заполните переменные окружения
3. Получите токен бота у [@BotFather](https://t.me/BotFather)
```

### 3. Используйте pre-commit hooks (опционально)

Установите `git-secrets` или `pre-commit` для автоматической проверки:

```bash
# Установка git-secrets
brew install git-secrets  # macOS
# или
git clone https://github.com/awslabs/git-secrets.git

# Настройка
git secrets --install
git secrets --register-aws  # или настройте свои паттерны
```

### 4. Регулярно проверяйте историю

Периодически проверяйте историю Git на наличие секретов:

```bash
# Поиск токенов в истории
git log --all --full-history --source -- "*token*" "*secret*"
```

---

## 🆘 Что делать при утечке данных

1. **Немедленно отзовите токен** в BotFather
2. **Удалите токен из репозитория** (см. инструкции выше)
3. **Обновите все секреты** (токены, пароли, ключи)
4. **Проверьте логи** на подозрительную активность
5. **Уведомите пользователей** (если затронуты их данные)

---

## 📚 Дополнительные ресурсы

- [GitHub: Removing sensitive data](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [OWASP: Secrets Management](https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_cryptographic_key)
- [12 Factor App: Config](https://12factor.net/config)

---

**Помните**: Безопасность - это не разовое действие, а постоянный процесс. Регулярно проверяйте и обновляйте меры безопасности!

