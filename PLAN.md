# 📋 План разработки: Telegram Bot Game «Крестики-Нолики»

## 🎯 Цель проекта
Создать полнофункциональное мини-приложение в Telegram с ботом, где пользователь может играть в крестики-нолики с ботом и получать промокоды за победу.

---

## 📦 ЭТАП 1: Инициализация проекта и структура

### Задачи:
- [ ] 1.1. Создать структуру директорий проекта
- [ ] 1.2. Создать базовые конфигурационные файлы
- [ ] 1.3. Настроить `.gitignore`
- [ ] 1.4. Создать `README.md` с описанием проекта

### Файлы для создания:
```
tic-tac-toe-bot/
├── dist/                    # Production файлы
│   ├── data/               # JSON файлы (создать пустые)
│   ├── logs/               # Логи
│   ├── cache/              # Кэш
│   └── assets/             # Статические файлы
├── config/
├── includes/
├── .gitignore
├── README.md
└── composer.json
```

### Критерии готовности:
✅ Структура директорий создана  
✅ Базовые файлы на месте  
✅ `.gitignore` настроен

---

## ⚙️ ЭТАП 2: Базовая конфигурация и инфраструктура

### Задачи:
- [ ] 2.1. Создать `dist/config.php` с базовыми настройками
- [ ] 2.2. Создать `dist/template-config.php` (класс `TemplateConfig`)
- [ ] 2.3. Создать `dist/includes/Logger.php` (система логирования)
- [ ] 2.4. Создать `dist/includes/Security.php` (безопасность, валидация)
- [ ] 2.5. Создать `dist/includes/functions.php` (общие функции)
- [ ] 2.6. Создать `dist/composer.json` (если нужны зависимости)

### Детали реализации:

#### 2.1. config.php
```php
<?php
require_once 'template-config.php';

define('BOT_TOKEN', TemplateConfig::BOT_TOKEN);
define('DOMAIN', TemplateConfig::DOMAIN);
define('FULL_URL', TemplateConfig::FULL_URL);
```

#### 2.2. template-config.php
```php
class TemplateConfig {
    const BOT_TOKEN = 'YOUR_BOT_TOKEN';
    const DOMAIN = 'your-domain.com';
    const FULL_URL = 'https://your-domain.com';
    const PROJECT_NAME = 'Крестики-Нолики';
    // ... остальные константы
}
```

#### 2.3. Logger.php
- Статические методы: `info()`, `error()`, `warning()`, `userAction()`
- Запись в `logs/app.log`
- Ротация логов
- JSON формат записей

#### 2.4. Security.php
- `validateInput()` - валидация данных
- `checkRateLimit()` - rate limiting
- `generateCSRFToken()` - CSRF защита
- `normalizePhone()` - нормализация (если понадобится)

### Критерии готовности:
✅ Все базовые классы созданы и работают  
✅ Логирование функционирует  
✅ Конфигурация загружается корректно

---

## 🤖 ЭТАП 3: Telegram Bot (Webhook и команды)

### Задачи:
- [ ] 3.1. Создать `dist/webhook.php` (обработчик webhook)
- [ ] 3.2. Создать `dist/bot/messages.php` (тексты сообщений)
- [ ] 3.3. Создать `dist/bot/bot-handler.php` (обработка команд)
- [ ] 3.4. Создать `dist/setup/setup-webhook.php` (настройка webhook)
- [ ] 3.5. Протестировать команду `/start` и отправку сообщений

### Детали реализации:

#### 3.1. webhook.php
```php
<?php
require_once 'config.php';
require_once 'Logger.php';

$input = file_get_contents('php://input');
$update = json_decode($input, true);

// Обработка сообщений
if (isset($update['message'])) {
    // Обработка команд
}

// Обработка callback queries
if (isset($update['callback_query'])) {
    // Обработка нажатий кнопок
}
```

#### 3.2. messages.php
```php
class BotMessages {
    const WELCOME = "Добро пожаловать 💕\nСыграем в крестики-нолики?";
    public static function win($code) { return "🎉 Победа! Промокод: {$code}"; }
    const LOSE = "Проигрыш";
    const DRAW = "Ничья 💫";
}
```

#### 3.3. bot-handler.php
- Функция `sendMessage()` - отправка через CURL
- Функция `answerCallbackQuery()` - ответ на callback
- Обработка команды `/start` с кнопкой WebApp
- Обработка команды `/help`

#### 3.4. setup-webhook.php
- Установка webhook через Telegram API
- Проверка статуса webhook
- Установка команд бота (`setMyCommands`)

### Критерии готовности:
✅ Webhook принимает обновления от Telegram  
✅ Команда `/start` работает и показывает кнопку  
✅ Сообщения отправляются корректно

---

## 🎮 ЭТАП 4: Игровая логика (Backend)

### Задачи:
- [ ] 4.1. Создать `dist/game/game-logic.php` (основная логика)
- [ ] 4.2. Создать `dist/game/ai.php` (AI бота)
- [ ] 4.3. Создать `dist/game/game-state.php` (управление состоянием)
- [ ] 4.4. Создать `dist/bot/promo.php` (генерация промокодов)
- [ ] 4.5. Создать `dist/includes/GameStorage.php` (работа с JSON)
- [ ] 4.6. Создать API endpoint `dist/api/game.php` (AJAX обработка)

### Детали реализации:

#### 4.1. game-logic.php
```php
class GameLogic {
    public static function checkWin($board, $symbol): bool {
        // Проверка строк, столбцов, диагоналей
    }
    
    public static function checkDraw($board): bool {
        // Проверка ничьей
    }
    
    public static function validateMove($board, $position): bool {
        // Валидация хода
    }
}
```

#### 4.2. ai.php
```php
class GameAI {
    public static function makeMove($board, $bot_symbol = 'O') {
        $random = mt_rand(1, 100);
        
        if ($random <= 10) {
            return self::findWinningMove($board, $bot_symbol);
        } elseif ($random <= 40) {
            return self::findBlockingMove($board, $bot_symbol);
        } else {
            return self::findRandomMove($board);
        }
    }
    
    private static function findWinningMove($board, $symbol) { }
    private static function findBlockingMove($board, $bot_symbol) { }
    private static function findRandomMove($board) { }
}
```

#### 4.3. game-state.php
- Управление состоянием игры
- Сохранение текущей игры
- Восстановление игры

#### 4.4. promo.php
```php
class PromoCode {
    public static function generate($tg_id, $game_id): string {
        // Генерация уникального 5-значного кода
        // Сохранение в promo-codes.json
    }
    
    private static function getUsedCodes(): array { }
    private static function savePromoCode($code, $tg_id, $game_id) { }
}
```

#### 4.5. GameStorage.php
```php
class GameStorage {
    public static function saveGame($game_data) {
        // Сохранение в games.json
    }
    
    public static function loadGames($tg_id = null) {
        // Загрузка игр из games.json
    }
    
    public static function getStatistics($tg_id) {
        // Статистика игрока
    }
}
```

#### 4.6. api/game.php
- Обработка AJAX запросов
- Валидация данных
- Вызов игровой логики
- Возврат JSON ответов

### Критерии готовности:
✅ Игровая логика работает корректно  
✅ AI делает ходы по правилам (60/30/10)  
✅ Промокоды генерируются уникально  
✅ Данные сохраняются в JSON файлы

---

## 🎨 ЭТАП 5: Web App (Frontend)

### Задачи:
- [ ] 5.1. Создать `dist/web/game.php` (главная страница игры)
- [ ] 5.2. Создать `dist/web/assets/css/variables.css` (дизайн-система)
- [ ] 5.3. Создать `dist/web/assets/css/main.css` (основные стили)
- [ ] 5.4. Создать `dist/web/assets/js/game.js` (клиентская логика)
- [ ] 5.5. Создать `dist/web/assets/js/telegram-api.js` (интеграция с Telegram)
- [ ] 5.6. Создать `dist/web/assets/js/promo.js` (отображение промокода)

### Детали реализации:

#### 5.1. game.php
- HTML структура игрового поля 3x3
- Инициализация Telegram WebApp SDK
- Подключение CSS и JS файлов
- Обработка параметра `tg_id`

#### 5.2. variables.css
```css
:root {
  --color-bg: #FFF7F3;
  --color-primary: #E89CA9;
  --color-secondary: #BFA2DB;
  --color-accent: #FFD6A5;
  --color-text-main: #4A4A4A;
  --color-x: #E89CA9;
  --color-o: #BFA2DB;
}
```

#### 5.3. main.css
- Стили игрового поля
- Стили кнопок
- Анимации
- Адаптивность (mobile-first)

#### 5.4. game.js
```javascript
class TicTacToeGame {
    constructor() {
        this.board = ['', '', '', '', '', '', '', '', ''];
        this.currentPlayer = 'X';
        this.gameOver = false;
    }
    
    makeMove(position) {
        // Логика хода игрока
        // Отправка на сервер
        // Ожидание хода бота
    }
    
    checkWin() { }
    checkDraw() { }
    resetGame() { }
}
```

#### 5.5. telegram-api.js
```javascript
// Инициализация Telegram WebApp
if (window.Telegram && window.Telegram.WebApp) {
    const tg = window.Telegram.WebApp;
    tg.ready();
    tg.expand();
    
    // Отправка данных в бот
    function sendToBot(data) {
        tg.sendData(JSON.stringify(data));
    }
}
```

#### 5.6. promo.js
- Отображение промокода при победе
- Анимация появления
- Копирование в буфер обмена

### Критерии готовности:
✅ Игровое поле отображается корректно  
✅ Ходы обрабатываются и отображаются  
✅ Telegram WebApp SDK интегрирован  
✅ Промокод отображается при победе

---

## 🔗 ЭТАП 6: Интеграция компонентов

### Задачи:
- [ ] 6.1. Интегрировать frontend с backend API
- [ ] 6.2. Настроить отправку промокода в бот через WebApp
- [ ] 6.3. Обработать результаты игры в webhook
- [ ] 6.4. Добавить обработку ошибок и валидацию
- [ ] 6.5. Протестировать полный цикл игры

### Детали реализации:

#### 6.1. Интеграция Frontend-Backend
- AJAX запросы из `game.js` в `api/game.php`
- Обработка ответов и обновление UI
- Обработка ошибок

#### 6.2. Отправка промокода
```javascript
// В game.js после победы
if (result.win && result.promo_code) {
    showPromoCode(result.promo_code);
    
    // Отправка в бот
    if (window.Telegram && window.Telegram.WebApp) {
        window.Telegram.WebApp.sendData(JSON.stringify({
            action: 'win',
            promo_code: result.promo_code,
            game_id: result.game_id
        }));
    }
}
```

#### 6.3. Обработка в webhook
```php
// В webhook.php
if (isset($update['message']['web_app_data'])) {
    $data = json_decode($update['message']['web_app_data']['data'], true);
    
    if ($data['action'] === 'win') {
        // Отправка сообщения с промокодом
        sendMessage($chat_id, BotMessages::win($data['promo_code']));
    }
}
```

#### 6.4. Обработка ошибок
- Валидация всех входных данных
- Try-catch блоки для критических операций
- Логирование ошибок
- Понятные сообщения пользователю

### Критерии готовности:
✅ Полный цикл игры работает  
✅ Промокод отправляется в бот  
✅ Ошибки обрабатываются корректно  
✅ Данные сохраняются в JSON

---

## 🧪 ЭТАП 7: Тестирование и отладка

### Задачи:
- [ ] 7.1. Протестировать все команды бота
- [ ] 7.2. Протестировать игровую логику (все сценарии)
- [ ] 7.3. Протестировать AI бота
- [ ] 7.4. Протестировать генерацию промокодов
- [ ] 7.5. Протестировать на разных устройствах
- [ ] 7.6. Проверить логи и исправить ошибки

### Тестовые сценарии:

#### 7.1. Команды бота
- `/start` - приветствие и кнопка
- `/help` - справка
- Неизвестные команды

#### 7.2. Игровая логика
- Победа игрока (все комбинации)
- Победа бота
- Ничья
- Некорректные ходы

#### 7.3. AI бота
- Выигрышный ход (10%)
- Блокировка (30%)
- Случайный ход (60%)

#### 7.4. Промокоды
- Уникальность кодов
- Сохранение в JSON
- Отправка в бот

### Критерии готовности:
✅ Все тесты пройдены  
✅ Нет критических ошибок  
✅ Логи чистые

---

## 🚀 ЭТАП 8: Подготовка к деплою

### Задачи:
- [ ] 8.1. Создать `DEPLOYMENT.md` с инструкциями
- [ ] 8.2. Настроить права доступа к файлам
- [ ] 8.3. Создать пустые JSON файлы с правильной структурой
- [ ] 8.4. Настроить `.htaccess` (если нужен)
- [ ] 8.5. Проверить все пути и URL

### Детали:

#### 8.1. DEPLOYMENT.md
- Требования к серверу
- Инструкция по установке
- Настройка webhook
- Проверка работы

#### 8.2. Права доступа
- `data/` - 755 (директория), 644 (файлы)
- `logs/` - 755 (директория), 644 (файлы)
- PHP файлы - 644

#### 8.3. JSON файлы
```json
// data/games.json
[]

// data/promo-codes.json
[]

// data/statistics.json
{}
```

#### 8.4. .htaccess
- Редирект на HTTPS
- Защита JSON файлов (если нужно)
- Настройки PHP

### Критерии готовности:
✅ Все файлы готовы к деплою  
✅ Инструкции написаны  
✅ Структура данных корректна

---

## 🌐 ЭТАП 9: Деплой и настройка

### Задачи:
- [ ] 9.1. Загрузить файлы на сервер
- [ ] 9.2. Настроить конфигурацию (токены, домен)
- [ ] 9.3. Установить webhook через `setup-webhook.php`
- [ ] 9.4. Проверить работу бота
- [ ] 9.5. Протестировать игру в продакшн
- [ ] 9.6. Настроить мониторинг логов

### Детали:

#### 9.1. Загрузка файлов
- Загрузить все файлы из `dist/`
- Проверить структуру директорий
- Убедиться, что все файлы на месте

#### 9.2. Конфигурация
- Обновить `template-config.php` с реальными данными
- Установить BOT_TOKEN
- Установить DOMAIN и FULL_URL

#### 9.3. Webhook
```bash
# Выполнить в браузере
https://your-domain.com/setup/setup-webhook.php
```

#### 9.4. Проверка
- Отправить `/start` боту
- Проверить ответ
- Проверить логи

#### 9.5. Тестирование
- Сыграть полную игру
- Проверить генерацию промокода
- Проверить отправку в бот

### Критерии готовности:
✅ Бот работает в продакшн  
✅ Игра функционирует корректно  
✅ Промокоды генерируются и отправляются  
✅ Логи пишутся

---

## 📊 ЭТАП 10: Финальная проверка и оптимизация

### Задачи:
- [ ] 10.1. Провести финальное тестирование всех функций
- [ ] 10.2. Оптимизировать производительность
- [ ] 10.3. Проверить безопасность
- [ ] 10.4. Обновить документацию
- [ ] 10.5. Подготовить финальный отчет

### Детали:

#### 10.1. Финальное тестирование
- Все команды бота
- Все сценарии игры
- Генерация промокодов
- Отправка в бот
- Обработка ошибок

#### 10.2. Оптимизация
- Минимизация CSS/JS
- Оптимизация изображений
- Кэширование (если нужно)
- Оптимизация JSON операций

#### 10.3. Безопасность
- Проверка валидации
- Проверка rate limiting
- Проверка логирования
- Проверка CSRF (если используется)

#### 10.4. Документация
- Обновить README.md
- Дополнить DEPLOYMENT.md
- Добавить комментарии в код

### Критерии готовности:
✅ Все функции работают стабильно  
✅ Производительность оптимизирована  
✅ Безопасность проверена  
✅ Документация актуальна

---

## ✅ Чеклист готовности к продакшн

### Функциональность
- [ ] Бот отвечает на команды
- [ ] Игра запускается через WebApp
- [ ] Игровая логика работает корректно
- [ ] AI делает ходы по правилам
- [ ] Промокоды генерируются уникально
- [ ] Промокоды отправляются в бот
- [ ] Все результаты сохраняются

### Технические требования
- [ ] Webhook настроен и работает
- [ ] SSL сертификат установлен
- [ ] Права доступа настроены
- [ ] Логирование работает
- [ ] Ошибки обрабатываются
- [ ] Валидация данных работает

### Безопасность
- [ ] Токены не в коде
- [ ] Валидация входных данных
- [ ] Rate limiting работает
- [ ] Логирование безопасности

### Документация
- [ ] README.md актуален
- [ ] DEPLOYMENT.md написан
- [ ] Комментарии в коде

---

## 📝 Примечания

1. **Последовательность**: Этапы должны выполняться строго по порядку
2. **Тестирование**: После каждого этапа проводить базовое тестирование
3. **Логирование**: Все действия логировать для отладки
4. **Безопасность**: Не забывать про валидацию и безопасность
5. **Документация**: Обновлять документацию по ходу разработки

---

## 🎯 Итоговый результат

После выполнения всех этапов должно быть готово:
- ✅ Работающий Telegram бот с командами
- ✅ WebApp с игрой в крестики-нолики
- ✅ AI бот с правильной логикой
- ✅ Генерация и отправка промокодов
- ✅ Сохранение данных в JSON
- ✅ Логирование и мониторинг
- ✅ Полная документация

**Проект готов к продакшн! 🚀**

