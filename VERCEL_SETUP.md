# 🚀 Настройка проекта на Vercel

## ✅ Что уже сделано автоматически

- ✅ Обновлен домен в `dist/template-config.php` на `tic-tac-toe-virid-two.vercel.app`
- ✅ Создан файл `vercel.json` для конфигурации Vercel
- ✅ Настроены маршруты для PHP файлов и статических ресурсов

## 📋 Что нужно сделать вручную

### 1. Настройка переменных окружения в Vercel

1. Откройте проект в Vercel Dashboard: https://vercel.com/dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

   ```
   BOT_TOKEN=ваш_токен_бота_от_BotFather
   ```

   ⚠️ **Важно**: Убедитесь, что переменная добавлена для всех окружений (Production, Preview, Development)

### 2. Настройка структуры проекта (если нужно)

Vercel автоматически определит структуру из `vercel.json`, но убедитесь что:
- Файлы из `dist/` будут доступны на сервере
- PHP файлы будут обрабатываться через serverless functions

### 3. Первый деплой

После подключения GitHub репозитория к Vercel:
1. Vercel автоматически запустит деплой при пуше в `main` ветку
2. Дождитесь завершения деплоя
3. Проверьте, что сайт доступен по адресу: `https://tic-tac-toe-virid-two.vercel.app`

### 4. Настройка webhook для Telegram бота

После успешного деплоя:

1. Откройте в браузере:
   ```
   https://tic-tac-toe-virid-two.vercel.app/setup/setup-webhook.php
   ```

2. Убедитесь, что webhook установлен успешно

3. Проверьте работу бота:
   - Откройте бота в Telegram: https://t.me/TicTacToe_ru_bot
   - Отправьте команду `/start`
   - Проверьте, что бот отвечает

### 5. Проверка работы игры

1. Откройте бота в Telegram
2. Нажмите кнопку "Играть" или отправьте команду `/start`
3. Проверьте, что игра открывается и работает корректно
4. Сыграйте партию и проверьте:
   - Сохранение игры
   - Генерацию промокодов при победе
   - Отображение истории игр

## 🔧 Технические детали

### Структура маршрутов в Vercel

- `/webhook.php` → обрабатывает обновления от Telegram
- `/api/game.php` → API для игровой логики
- `/setup/setup-webhook.php` → настройка webhook
- `/web/*` → статические файлы и игра (HTML, CSS, JS)
- `/web/game.php` → главная страница игры

### Особенности Vercel

1. **Serverless Functions**: PHP файлы обрабатываются как serverless functions
2. **Статические файлы**: CSS, JS, изображения обслуживаются как статические
3. **Переменные окружения**: Используйте Environment Variables в Vercel Dashboard
4. **Автоматический HTTPS**: Vercel автоматически предоставляет SSL сертификат

### Потенциальные проблемы и решения

#### Проблема: PHP файлы не обрабатываются
**Решение**: Убедитесь, что установлен `@vercel/php` в `vercel.json`

#### Проблема: Пути к файлам не работают
**Решение**: Проверьте маршруты в `vercel.json`, возможно нужно скорректировать пути

#### Проблема: Переменные окружения не работают
**Решение**: 
- Проверьте, что переменные добавлены в Vercel Dashboard
- Убедитесь, что переменные добавлены для нужного окружения (Production)
- Перезапустите деплой после добавления переменных

#### Проблема: Webhook не работает
**Решение**:
- Проверьте, что URL webhook правильный: `https://tic-tac-toe-virid-two.vercel.app/webhook.php`
- Убедитесь, что `BOT_TOKEN` установлен в переменных окружения
- Проверьте логи в Vercel Dashboard → Functions → Logs

## 📝 Проверочный список

- [ ] Переменная `BOT_TOKEN` добавлена в Vercel Environment Variables
- [ ] Проект успешно задеплоен на Vercel
- [ ] Сайт доступен по адресу `https://tic-tac-toe-virid-two.vercel.app`
- [ ] Webhook настроен через `/setup/setup-webhook.php`
- [ ] Бот отвечает на команды в Telegram
- [ ] Игра открывается и работает корректно
- [ ] Промокоды генерируются при победе
- [ ] История игр отображается правильно

## 🔗 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Документация Vercel PHP: https://vercel.com/docs/runtimes/php
- Telegram Bot API: https://core.telegram.org/bots/api

## ⚠️ Важные замечания

1. **Файлы данных**: JSON файлы (games.json, promo-codes.json) будут храниться в serverless функции. Для продакшена рекомендуется использовать внешнее хранилище (например, Vercel KV или внешнюю БД).

2. **Логи**: Логи будут доступны в Vercel Dashboard → Functions → Logs

3. **Производительность**: Serverless functions имеют ограничения по времени выполнения. Для тяжелых операций может потребоваться оптимизация.

4. **Холодный старт**: Первый запрос к serverless function может быть медленнее из-за "холодного старта".

