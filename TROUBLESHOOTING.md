# 🔍 Troubleshooting - Бот не отвечает

## ❌ Проблема: Бот не реагирует на /start

### Шаг 1: Проверьте webhook

Проверьте что webhook настроен правильно:

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

Должно вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-app.up.railway.app/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

**Если `url` пустой или неправильный:**
- Настройте webhook: откройте `https://your-app.up.railway.app/setup-webhook`
- Или вручную:
  ```bash
  curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
    -d "url=https://your-app.up.railway.app/webhook"
  ```

### Шаг 2: Проверьте логи Railway

1. Откройте Railway: https://railway.app
2. Выберите ваш проект
3. Перейдите на вкладку **"Deployments"**
4. Выберите последний деплой → **"View Logs"**

**Что искать:**
- ✅ Должны быть сообщения типа: `✅ Webhook сервер запущен на порту...`
- ✅ При отправке `/start` должны появиться логи обработки
- ❌ Если нет логов при отправке команды - webhook не работает

### Шаг 3: Проверьте что бот запущен

В логах Railway должны быть:
```
✅ Webhook сервер запущен на порту 3001
📡 Ожидание обновлений от Telegram...
```

Если этого нет - бот не запустился.

### Шаг 4: Проверьте переменные окружения

В Railway → Settings → Variables должны быть:
- ✅ `BOT_TOKEN` - правильный токен
- ✅ `WEB_APP_URL` - https://develonik.ru/
- ✅ `USE_WEBHOOK=true`
- ✅ `NODE_ENV=production`

### Шаг 5: Проверьте endpoint /webhook

Проверьте что endpoint доступен:

```bash
curl -X POST https://your-app.up.railway.app/webhook \
  -H "Content-Type: application/json" \
  -d '{"update_id":123,"message":{"message_id":1,"from":{"id":123,"first_name":"Test"},"chat":{"id":123},"date":1234567890,"text":"/start"}}'
```

Должен вернуть `{"ok":true}` или просто успешный ответ.

### Шаг 6: Проверьте обработчики команд

Убедитесь что обработчик `/start` зарегистрирован в `bot/handlers/commands.ts`.

## 🔧 Быстрые решения

### Решение 1: Перезапустить бота

В Railway:
1. Settings → Restart Service
2. Или удалите и создайте деплой заново

### Решение 2: Переустановить webhook

```bash
# Удалить webhook
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/deleteWebhook"

# Установить заново
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-app.up.railway.app/webhook"
```

### Решение 3: Проверить URL webhook

Убедитесь что URL правильный:
- ✅ Должен быть HTTPS
- ✅ Должен заканчиваться на `/webhook`
- ✅ Должен быть доступен (не 404)

## 📊 Диагностика через логи

Добавьте больше логирования в код для отладки (если нужно).

## ✅ Чеклист

- [ ] Webhook настроен (`getWebhookInfo` показывает правильный URL)
- [ ] Бот запущен (есть логи "Webhook сервер запущен")
- [ ] Переменные окружения установлены
- [ ] Endpoint `/webhook` доступен
- [ ] При отправке `/start` появляются логи в Railway
- [ ] Нет ошибок в логах Railway

