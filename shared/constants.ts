/**
 * Общие константы проекта
 */

/**
 * Размер игрового поля
 */
export const BOARD_SIZE = 3;

/**
 * Символы игроков
 */
export const PLAYER_X: 'X' = 'X';
export const PLAYER_O: 'O' = 'O';

/**
 * Все возможные выигрышные комбинации (индексы ячеек)
 * Формат: [row, col][]
 */
export const WINNING_COMBINATIONS: Array<Array<[number, number]>> = [
  // Горизонтальные линии
  [[0, 0], [0, 1], [0, 2]],
  [[1, 0], [1, 1], [1, 2]],
  [[2, 0], [2, 1], [2, 2]],
  // Вертикальные линии
  [[0, 0], [1, 0], [2, 0]],
  [[0, 1], [1, 1], [2, 1]],
  [[0, 2], [1, 2], [2, 2]],
  // Диагонали
  [[0, 0], [1, 1], [2, 2]],
  [[0, 2], [1, 1], [2, 0]],
];

/**
 * Стратегия AI бота
 * Проценты: случайный ход / блокировка / выигрышный ход
 */
export const AI_STRATEGY = {
  RANDOM: 0.6,      // 60% - случайный ход
  BLOCK: 0.3,       // 30% - блокировка игрока
  WIN: 0.1,         // 10% - выигрышный ход
} as const;

/**
 * Диапазон промокода
 */
export const PROMO_CODE_MIN = 10000;
export const PROMO_CODE_MAX = 99999;

