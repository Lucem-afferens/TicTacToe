name: Deploy TicTacToe Bot & Web App

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # ============================================
  # Job 1: Build and Test
  # ============================================
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run linter
      run: npm run lint || echo "Linter not configured yet"
      continue-on-error: true
      
    - name: Run tests
      run: npm test || echo "Tests not configured yet"
      continue-on-error: true
      
    - name: Build project
      run: npm run build
      
    - name: Verify build output
      run: |
        echo "📦 Проверка результата сборки..."
        if [ -d "dist/bot" ]; then
          echo "✅ Bot build успешен"
          ls -la dist/bot/ | head -5
        fi
        if [ -d "dist/web" ]; then
          echo "✅ Web App build успешен"
          ls -la dist/web/ | head -5
        fi

  # ============================================
  # Job 2: Deploy Web App and Bot to Beget
  # ============================================
  deploy:
    name: Deploy to Beget
    needs: build-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build project
      run: npm run build
      
    - name: Verify build output
      run: |
        echo "📦 Проверка результата сборки..."
        if [ -d "dist/web" ]; then
          echo "✅ Web App build успешен"
          ls -la dist/web/ | head -5
        fi
        if [ -d "dist/bot" ]; then
          echo "✅ Bot build успешен"
          ls -la dist/bot/ | head -5
        fi
      
    - name: Create .env file for bot
      run: |
        echo "🔐 Создание .env файла для бота..."
        mkdir -p dist/bot
        cat > dist/bot/.env << EOF
        BOT_TOKEN=${{ secrets.BOT_TOKEN }}
        BOT_USERNAME=${{ secrets.BOT_USERNAME || 'TicTacToe_ru_bot' }}
        WEB_APP_URL=${{ secrets.WEB_APP_URL || 'https://develonik.ru/' }}
        NODE_ENV=production
        EOF
        echo "✅ .env файл создан (безопасно, не коммитится в Git)"
        # Проверяем что токен есть (но не выводим его)
        if [ -z "${{ secrets.BOT_TOKEN }}" ]; then
          echo "⚠️ BOT_TOKEN не установлен в GitHub Secrets"
        else
          echo "✅ BOT_TOKEN установлен"
        fi
      
    - name: Deploy Web App to Beget FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/web/
        server-dir: ${{ secrets.FTP_WEB_PATH }}/
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/.DS_Store
          **/Thumbs.db
          **/*.map
        log-level: verbose
      
    - name: Set bot deployment path
      id: bot-path
      run: |
        if [ -n "${{ secrets.FTP_BOT_PATH }}" ]; then
          echo "path=${{ secrets.FTP_BOT_PATH }}" >> $GITHUB_OUTPUT
        else
          echo "path=${{ secrets.FTP_WEB_PATH }}/bot" >> $GITHUB_OUTPUT
        fi
      
    - name: Prepare bot deployment directory
      run: |
        echo "📦 Подготовка файлов для деплоя бота..."
        mkdir -p deploy-bot/bot deploy-bot/shared
        cp -r dist/bot/* deploy-bot/bot/ 2>/dev/null || true
        cp -r dist/shared/* deploy-bot/shared/ 2>/dev/null || true
        cp dist/bot/.env deploy-bot/bot/.env 2>/dev/null || true
        echo "✅ Файлы подготовлены"
        ls -la deploy-bot/bot/ | head -5
        ls -la deploy-bot/shared/ | head -5
      
    - name: Deploy Bot, Shared modules and .env to Beget FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./deploy-bot/
        server-dir: ${{ steps.bot-path.outputs.path }}/
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/.DS_Store
          **/Thumbs.db
        log-level: verbose
      
    - name: Prepare package files for bot
      run: |
        echo "📦 Подготовка package.json для бота..."
        mkdir -p deploy-bot
        cp package.json deploy-bot/package.json
        cp package-lock.json deploy-bot/package-lock.json 2>/dev/null || true
        echo "✅ Файлы подготовлены"
      
    - name: Deploy package files for bot
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_HOST }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./deploy-bot/
        server-dir: ${{ steps.bot-path.outputs.path }}/
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/.DS_Store
          **/Thumbs.db
          bot/**
          shared/**
        log-level: verbose
      
    - name: Deployment Success
      run: |
        echo "🎉 Деплой успешно завершен!"
        echo "🌐 Web App: https://develonik.ru/"
        echo "🤖 Bot: задеплоен в ${{ steps.bot-path.outputs.path }}/"
        echo "🔐 .env файл загружен на сервер (безопасно, не в Git)"
        echo ""
        echo "📋 Следующие шаги на сервере:"
        echo "   1. Подключитесь к серверу через SSH"
        echo "   2. Перейдите в директорию бота: cd ${{ steps.bot-path.outputs.path }}"
        echo "   3. Запустите автоматический скрипт: bash install-and-start.sh"
        echo ""
        echo "   Или вручную:"
        echo "   cd ${{ steps.bot-path.outputs.path }}"
        echo "   npm install --production"
        echo "   npm run build:bot"
        echo "   npm run start:webhook"
        echo ""
        echo "📅 Время: $(date)"

