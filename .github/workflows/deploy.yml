name: Deploy to Beget (DISABLED - Now using Vercel)

# ⚠️ ЭТОТ WORKFLOW ОТКЛЮЧЕН
# Проект теперь деплоится на Vercel автоматически через GitHub интеграцию
# Vercel автоматически деплоит при пуше в main/master
# Домен: https://tic-tac-toe-virid-two.vercel.app

on:
  # Отключено - не запускается автоматически
  # push:
  #   branches:
  #     - main
  #     - master
  workflow_dispatch: # Можно запустить вручную, если нужно

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
      
      - name: Create deployment directory
        run: |
          mkdir -p deploy
          # Копируем только файлы из dist
          cp -r dist/* deploy/ 2>/dev/null || true
          # Создаем необходимые директории (если их нет)
          mkdir -p deploy/data deploy/logs deploy/cache
          # НЕ создаем JSON файлы - они должны сохраняться на сервере
          # При первом использовании они создадутся автоматически
      
      - name: Create config.local.php from GitHub Secret
        run: |
          # Создаем локальный конфиг с токеном из GitHub Secrets
          # Этот файл НЕ попадает в git и существует только на сервере
          cat > deploy/config.local.php << 'EOF'
          <?php
          /**
           * Локальная конфигурация (создается при деплое)
           * Этот файл НЕ попадает в git и содержит секретные данные
           * Создается автоматически из GitHub Secrets
           */
          if (!defined('LOCAL_BOT_TOKEN')) {
              define('LOCAL_BOT_TOKEN', '${{ secrets.BOT_TOKEN }}');
          }
          EOF
          echo "✅ config.local.php created (token from GitHub Secrets)"
      
      - name: Deploy to Beget via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./deploy/
          server-dir: ${{ secrets.FTP_PATH }}/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.env
            **/.env.*
            **/composer.json
            **/composer.lock
            **/package.json
            **/package-lock.json
            **/.DS_Store
            **/README.md
            **/DEPLOYMENT.md
            **/PLAN.md
            **/запрос.md
            **/.cursor/**
            **/.gitignore
            **/.htaccess.bak
            **/*.md
            # НЕ исключаем config.local.php - он нужен на сервере
            # НЕ исключаем данные пользователей - они должны сохраняться на сервере
          dangerous-clean-slate: false
          dry-run: false
      
      - name: Setup webhook after deployment
        run: |
          echo "✅ Deployment completed!"
          echo "📝 Next: Run https://develonik.ru/setup/setup-webhook.php to configure webhook"
      
      - name: Deployment summary
        run: |
          echo "⚠️  ВНИМАНИЕ: Этот workflow отключен!"
          echo "📁 Проект теперь деплоится на Vercel автоматически"
          echo "🌐 Vercel Domain: https://tic-tac-toe-virid-two.vercel.app"
          echo "🌐 Старый домен (Beget): develonik.ru (больше не используется)"
          echo "🤖 Bot: @TicTacToe_ru_bot"
          echo ""
          echo "✅ Vercel автоматически деплоит при пуше в main/master"
          echo "📝 Настройка webhook: https://tic-tac-toe-virid-two.vercel.app/api/setup-webhook.js"
          echo "🔍 Проверка настроек: https://tic-tac-toe-virid-two.vercel.app/api/check-webhook"
          echo "🧪 Тест бота: https://t.me/TicTacToe_ru_bot"
