# 🔍 Проверка работы бота

## ✅ Быстрая проверка

### 1. Проверьте webhook

Выполните команду (замените `<BOT_TOKEN>` на ваш токен):

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

**Должно вернуть:**
```json
{
  "ok": true,
  "result": {
    "url": "https://your-app.up.railway.app/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "last_error_date": 0,
    "last_error_message": "",
    "max_connections": 40
  }
}
```

**Если `url` пустой или неправильный:**
- Откройте: `https://your-app.up.railway.app/setup-webhook`
- Или настройте вручную (см. ниже)

### 2. Проверьте логи Railway

1. Откройте Railway: https://railway.app
2. Выберите ваш проект
3. Перейдите на **"Deployments"** → выберите последний деплой → **"View Logs"**

**Что должно быть:**
```
✅ Webhook сервер запущен на порту 3001
📡 Ожидание обновлений от Telegram...
```

**Теперь отправьте `/start` боту и проверьте логи:**
- Должны появиться: `📨 Получено обновление от Telegram`
- Должны появиться: `📨 Получена команда /start`
- Должны появиться: `✅ Ответ на /start отправлен`

**Если логов нет при отправке команды:**
- Webhook не настроен или неправильный URL
- Настройте webhook (см. ниже)

### 3. Настройте webhook вручную

Если webhook не настроен, выполните:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-app.up.railway.app/webhook" \
  -d "drop_pending_updates=true"
```

Замените:
- `<BOT_TOKEN>` на ваш токен
- `your-app.up.railway.app` на ваш Railway домен

### 4. Проверьте что endpoint работает

```bash
curl -X POST https://your-app.up.railway.app/webhook \
  -H "Content-Type: application/json" \
  -d '{"update_id":123,"message":{"message_id":1,"from":{"id":123,"first_name":"Test"},"chat":{"id":123},"date":1234567890,"text":"/start"}}'
```

Должен вернуть `{"ok":true}` или успешный ответ.

## 🔧 Частые проблемы

### Проблема: Webhook не настроен

**Решение:**
1. Откройте `https://your-app.up.railway.app/setup-webhook`
2. Или настройте вручную через curl (см. выше)

### Проблема: Логи есть, но бот не отвечает

**Возможные причины:**
1. Ошибка в обработчике команд - проверьте логи Railway
2. Проблема с отправкой сообщения - проверьте токен
3. Бот заблокирован - проверьте статус бота в BotFather

### Проблема: Webhook URL неправильный

**Проверьте:**
- URL должен быть HTTPS (не HTTP)
- URL должен заканчиваться на `/webhook`
- URL должен быть доступен (не 404)

**Railway автоматически создаёт домен:**
- Проверьте в Railway → Settings → Networking → Domains
- Используйте этот домен для webhook

## 📊 Диагностика через Telegram API

### Проверка информации о боте:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getMe"
```

### Проверка webhook:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

### Удаление webhook (если нужно):
```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/deleteWebhook?drop_pending_updates=true"
```

### Установка webhook:
```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-app.up.railway.app/webhook" \
  -d "drop_pending_updates=true" \
  -d "allowed_updates=[\"message\",\"callback_query\"]"
```

## ✅ Чеклист

- [ ] Webhook настроен (`getWebhookInfo` показывает правильный URL)
- [ ] Бот запущен (есть логи "Webhook сервер запущен")
- [ ] При отправке `/start` появляются логи в Railway
- [ ] Endpoint `/webhook` доступен и отвечает
- [ ] Нет ошибок в логах Railway
- [ ] Токен бота правильный (`getMe` возвращает информацию о боте)

