# 📌 Проект: Telegram Bot Game «Крестики-Нолики»

## 1. Общая концепция проекта

### Технический стэк
- **Язык программирования**: PHP 7.4+ (полностью идентичен проекту prize-wheel)
- **Архитектура**: Telegram Bot Webhook + PHP Web Application
- **Хранение данных**: JSON файлы (file-based storage)
- **Frontend**: HTML/CSS/JavaScript (Telegram WebApp)
- **API**: Telegram Bot API через CURL

### Описание проекта
**Тип**: Telegram bot с интегрированной веб-игрой  
**Игра**: Классические крестики-нолики (игрок vs бот)  
**Целевая аудитория**: Женщины 25–40 лет  
**Тон общения**: Дружелюбный, эстетичный, мягкий, lifestyle/beauty

### Ключевая механика

1. **Вход в систему**
   - Пользователь открывает Telegram-бота
   - Нажимает кнопку «Играть» (WebApp)
   - Открывается игровое поле в Telegram WebApp

2. **Игровой процесс**
   - Игрок делает ход первым (символ ❌)
   - Бот отвечает автоматически (символ ⭕)
   - Игра продолжается до победы/проигрыша/ничьей

3. **Результаты игры**
   - **Победа игрока**:
     - Генерируется уникальный 5-значный промокод
     - Промокод отображается на экране игры
     - Промокод отправляется в Telegram-бот через WebApp API
     - Сохранение результата в JSON файл
   
   - **Проигрыш**:
     - Сообщение в Telegram-боте: "Проигрыш"
     - На экране игры: кнопка «Сыграть ещё раз»
     - Сохранение статистики игры
   
   - **Ничья**:
     - Сообщение: "Ничья 💫"
     - Кнопка «Сыграть ещё раз»

## 2. Архитектура проекта

### Структура директорий (PHP стэк)

```
tic-tac-toe-bot/
│
├── dist/                          # Production файлы (как в prize-wheel)
│   ├── webhook.php               # Обработчик Telegram webhook
│   ├── config.php                # Основная конфигурация
│   ├── template-config.php       # Настройки шаблона (класс TemplateConfig)
│   │
│   ├── bot/                       # Логика Telegram бота
│   │   ├── messages.php          # Тексты сообщений (массив/класс)
│   │   ├── promo.php             # Генерация промокодов (класс PromoCode)
│   │   └── bot-handler.php       # Обработка команд бота
│   │
│   ├── game/                     # Логика игры
│   │   ├── game-logic.php        # Основная логика крестиков-ноликов
│   │   ├── ai.php                # AI бота-соперника (класс GameAI)
│   │   └── game-state.php        # Управление состоянием игры
│   │
│   ├── web/                      # Веб-приложение
│   │   ├── index.php             # Главная страница (игровое поле)
│   │   ├── game.php              # Страница игры (WebApp)
│   │   │
│   │   ├── assets/
│   │   │   ├── css/
│   │   │   │   ├── variables.css # Дизайн-система (CSS переменные)
│   │   │   │   └── main.css      # Основные стили
│   │   │   │
│   │   │   ├── js/
│   │   │   │   ├── game.js       # Клиентская логика игры
│   │   │   │   ├── telegram-api.js # Интеграция с Telegram WebApp SDK
│   │   │   │   └── promo.js      # Отображение промокода
│   │   │   │
│   │   │   └── images/
│   │   │       ├── icons/
│   │   │       └── backgrounds/
│   │
│   ├── includes/                 # Вспомогательные классы
│   │   ├── Logger.php            # Система логирования (как в prize-wheel)
│   │   ├── Security.php          # Безопасность, валидация
│   │   ├── GameStorage.php       # Работа с JSON файлами (игры, статистика)
│   │   └── functions.php         # Общие функции
│   │
│   ├── data/                      # JSON файлы с данными
│   │   ├── games.json            # История игр
│   │   ├── promo-codes.json      # Выданные промокоды
│   │   ├── statistics.json       # Статистика игр
│   │   └── users.json            # Пользователи (если нужна регистрация)
│   │
│   ├── logs/                     # Логи приложения
│   │   └── app.log
│   │
│   ├── cache/                     # Кэш (если нужен)
│   │
│   └── setup/                    # Скрипты настройки
│       ├── setup-webhook.php     # Настройка Telegram webhook
│       └── init-files.php        # Инициализация файлов
│
├── config/                       # Конфигурационные файлы
│   └── database.php              # (если понадобится БД в будущем)
│
├── includes/                     # Общие файлы (source)
│   └── functions.php
│
├── composer.json                 # PHP зависимости
├── package.json                  # (если нужен npm для сборки)
├── README.md
└── DEPLOYMENT.md                 # Инструкция по развертыванию
```

## 3. Пользовательский сценарий (User Flow)

```
START
 ↓
Telegram Bot → /start
 ↓
Сообщение:
"Добро пожаловать 💕
Готовы сыграть в крестики-нолики?"
[Кнопка: Играть] (WebApp)
 ↓
Открытие Web App (game.php?tg_id={chat_id})
 ↓
Инициализация Telegram WebApp SDK
 ↓
Игровое поле 3x3 (пустое)
 ↓
Игрок делает ход (клик на ячейку)
 ↓
Отправка хода на сервер (AJAX → game-logic.php)
 ↓
Ход бота (AI вычисляет ответ)
 ↓
Обновление игрового поля
 ↓
Проверка результата
 ├─ Победа игрока
 │   ├─ Генерация промокода (PromoCode::generate())
 │   ├─ Сохранение в promo-codes.json
 │   ├─ Показ кода на экране
 │   ├─ Отправка в Telegram через WebApp.sendData()
 │   └─ Сообщение в бот:
 │       "🎉 Победа! Промокод выдан: 48291"
 │
 ├─ Проигрыш
 │   ├─ Сохранение статистики
 │   ├─ Сообщение в бот: "Проигрыш"
 │   └─ Экран:
 │       "Вы проиграли битву, но не войну! 💫"
 │       [Кнопка: Попробовать ещё раз]
 │
 └─ Ничья
     ├─ Сохранение статистики
     └─ Экран:
         "Ничья 💫"
         [Кнопка: Сыграть ещё раз]
```

## 4. Логика игры (Game Logic)

### Правила игры
- **Игрок**: Всегда ходит первым, символ ❌
- **Бот**: Играет вторым, символ ⭕
- **Размер поля**: 3x3 (9 ячеек)
- **Условие победы**: 3 символа в ряд (горизонталь, вертикаль, диагональ)
- **Условие ничьей**: Все ячейки заполнены, но нет победителя

### AI поведение бота
Бот играет **не идеально**, с элементом случайности для более интересной игры:

- **60%** — случайный ход (из доступных ячеек)
- **30%** — блокировка игрока (если игрок может выиграть следующим ходом)
- **10%** — выигрышный ход (если бот может выиграть)

### Реализация (PHP)

```php
// game/ai.php
class GameAI {
    /**
     * Вычисляет ход бота на основе текущего состояния игры
     */
    public static function makeMove($board, $bot_symbol = 'O') {
        $random = mt_rand(1, 100);
        
        if ($random <= 10) {
            // 10% - выигрышный ход
            return self::findWinningMove($board, $bot_symbol);
        } elseif ($random <= 40) {
            // 30% - блокировка игрока
            return self::findBlockingMove($board, $bot_symbol);
        } else {
            // 60% - случайный ход
            return self::findRandomMove($board);
        }
    }
}
```

## 5. Генерация промокода

### Формат промокода
- **Длина**: 5 символов
- **Тип**: Только цифры (10000-99999)
- **Уникальность**: Каждая победа = новый уникальный код
- **Хранение**: JSON файл `promo-codes.json`

### Реализация (PHP)

```php
// bot/promo.php
class PromoCode {
    /**
     * Генерирует уникальный промокод
     */
    public static function generate() {
        $promo_file = 'data/promo-codes.json';
        $used_codes = self::getUsedCodes();
        
        do {
            $code = str_pad(mt_rand(10000, 99999), 5, '0', STR_PAD_LEFT);
        } while (in_array($code, $used_codes));
        
        // Сохраняем промокод
        self::savePromoCode($code);
        
        return $code;
    }
}
```

### Условия выдачи
- Генерируется **только при победе игрока**
- Один код = одна победа
- Коды не повторяются (проверка уникальности)
- Отправляется:
  - В Web UI (отображение на экране)
  - В Telegram-бот (через WebApp.sendData() и webhook)

## 6. Telegram Bot — сообщения и команды

### Структура сообщений (PHP)

```php
// bot/messages.php
class BotMessages {
    const WELCOME = "Добро пожаловать 💕\nСыграем в крестики-нолики?";
    
    public static function win($code) {
        return "🎉 Победа! Промокод выдан: {$code}";
    }
    
    const LOSE = "Проигрыш";
    const DRAW = "Ничья 💫";
    const HELP = "Помощь по игре...";
}
```

### Команды бота
- `/start` — Приветствие и кнопка «Играть»
- `/help` — Справка по игре
- `/stats` — Статистика игрока (если реализована регистрация)

### Webhook обработка

```php
// webhook.php (как в prize-wheel)
if (isset($update['message'])) {
    $text = $update['message']['text'] ?? '';
    
    switch ($text) {
        case '/start':
            // Отправка приветствия с кнопкой WebApp
            break;
    }
}
```

## 7. Дизайн-система (Design System)

### Цветовая палитра
Ориентация на женскую аудиторию 25–40 лет:

```css
:root {
  --color-bg: #FFF7F3;           /* Мягкий бежевый фон */
  --color-primary: #E89CA9;     /* Розовый основной */
  --color-secondary: #BFA2DB;   /* Сиреневый вторичный */
  --color-accent: #FFD6A5;       /* Персиковый акцент */
  --color-text-main: #4A4A4A;    /* Темно-серый текст */
  --color-text-light: #8E8E8E;  /* Светло-серый текст */
  --color-x: #E89CA9;            /* Розовый для ❌ */
  --color-o: #BFA2DB;            /* Сиреневый для ⭕ */
}
```

### Типографика
- **Основной шрифт**: Inter / Montserrat (sans-serif)
- **Акцентный шрифт**: Playfair Display (заголовки, serif)

```css
body {
  font-family: 'Inter', sans-serif;
}

h1, h2, .title {
  font-family: 'Playfair Display', serif;
}
```

### Компоненты UI

#### Кнопки
```css
.button {
  background: var(--color-primary);
  color: white;
  border-radius: 16px;
  padding: 14px 20px;
  font-size: 16px;
  border: none;
  transition: all 0.3s ease;
}

.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(232, 156, 169, 0.4);
}
```

#### Игровое поле
- Сетка 3x3 с мягкими скруглениями
- Плавные анимации при ходе
- Ховеры без резких эффектов
- Визуальная обратная связь на каждое действие

## 8. UI-состояния игры

### Победа
```
🎉 Победа!
Твой промокод:
[48291] (крупно, выделено)

[Кнопка: Сыграть ещё]
```

### Проигрыш
```
Вы проиграли битву, но не войну! 💫

[Кнопка: Попробовать ещё раз]
```

### Ничья
```
Ничья 💫

[Кнопка: Сыграть ещё раз]
```

## 9. Технические требования

### Backend (PHP)
- **PHP версия**: 7.4+
- **Расширения**: json, curl, mbstring, session
- **Архитектура**: 
  - Webhook для Telegram Bot API
  - RESTful endpoints для игры (AJAX)
  - JSON файлы для хранения данных
  - Сессии для управления состоянием

### Frontend
- **Адаптивность**: Mobile-first подход
- **Telegram WebApp SDK**: Обязательная интеграция
- **Производительность**: Минимальный вес ассетов, оптимизация изображений
- **Совместимость**: Работа в Telegram WebView

### Безопасность
- Валидация всех пользовательских данных
- Rate limiting для API запросов
- CSRF защита (если нужна авторизация)
- Логирование всех действий (Logger класс)
- Проверка initData от Telegram WebApp

### Хранение данных
- **Формат**: JSON файлы
- **Структура**: 
  - `games.json` — история игр
  - `promo-codes.json` — выданные промокоды
  - `statistics.json` — статистика
- **Резервное копирование**: Автоматическое (как в prize-wheel)

## 10. Интеграция с Telegram

### Webhook настройка
```php
// setup/setup-webhook.php
$webhook_url = 'https://your-domain.com/webhook.php';
$bot_token = BOT_TOKEN;

// Установка webhook через Telegram API
```

### WebApp кнопка
```php
// В webhook.php при команде /start
$keyboard = [
    'inline_keyboard' => [
        [
            [
                'text' => '🎮 Играть',
                'web_app' => ['url' => "https://your-domain.com/game.php?tg_id={$chat_id}"]
            ]
        ]
    ]
];
```

### Отправка данных из WebApp
```javascript
// В game.js после победы
if (window.Telegram && window.Telegram.WebApp) {
    const promoCode = result.promo_code;
    window.Telegram.WebApp.sendData(JSON.stringify({
        action: 'win',
        promo_code: promoCode
    }));
}
```

## 11. Логирование и мониторинг

### Система логирования (как в prize-wheel)
```php
// includes/Logger.php
Logger::info('Game started', ['tg_id' => $tg_id]);
Logger::error('Game error', ['error' => $error]);
Logger::userAction('Game won', $user_id, ['promo_code' => $code]);
```

### Логируемые события
- Начало игры
- Ходы игрока и бота
- Результаты игры (победа/проигрыш/ничья)
- Генерация промокодов
- Ошибки и исключения

## 12. Развертывание

### Требования к серверу
- PHP 7.4+
- SSL сертификат (HTTPS обязателен для Telegram WebApp)
- Права на запись в директории `data/` и `logs/`
- Поддержка CURL для Telegram API

### Процесс деплоя
1. Загрузка файлов на сервер
2. Настройка webhook через `setup-webhook.php`
3. Проверка прав доступа к файлам
4. Тестирование бота и игры

---

**Примечание**: Проект полностью следует архитектуре и паттернам проекта prize-wheel для обеспечения консистентности и надежности кода.
