# Дизайн-система проекта «Крестики-Нолики»

## 🎯 Концепция дизайна

**Целевая аудитория**: Женщины 25–40 лет  
**Тон**: Дружелюбный, эстетичный, мягкий, «lifestyle / beauty»  
**Философия**: Мягкость, элегантность, приятные эмоции

---

## 🎨 Цветовая палитра

### Основные цвета

```css
:root {
  /* Фон */
  --color-bg: #FFF7F3;           /* Тёплый кремовый фон */
  --color-bg-secondary: #FFFFFF;  /* Белый для карточек */
  
  /* Основные акценты */
  --color-primary: #E89CA9;      /* Мягкий розовый - основной CTA */
  --color-secondary: #BFA2DB;    /* Сиреневый - вторичные элементы */
  --color-accent: #FFD6A5;       /* Персиковый - акценты */
  
  /* Текст */
  --color-text-main: #4A4A4A;    /* Основной текст */
  --color-text-light: #8E8E8E;   /* Второстепенный текст */
  --color-text-inverse: #FFFFFF;  /* Текст на цветном фоне */
  
  /* Игровые символы */
  --color-x: #E89CA9;            /* Крестик - мягкий розовый */
  --color-o: #BFA2DB;            /* Нолик - сиреневый */
  
  /* Состояния */
  --color-success: #A8D5BA;      /* Победа - мягкий зелёный */
  --color-error: #F5B7B1;        /* Проигрыш - мягкий красный */
  --color-warning: #FFD6A5;      /* Предупреждение */
  
  /* Границы и тени */
  --color-border: rgba(232, 156, 169, 0.2);
  --color-shadow: rgba(232, 156, 169, 0.15);
}
```

### Использование цветов

- **Primary** (`--color-primary`): Кнопки действий, активные элементы
- **Secondary** (`--color-secondary`): Вторичные кнопки, декоративные элементы
- **Accent** (`--color-accent`): Выделение важной информации
- **X/O**: Символы игроков должны быть контрастными и приятными

---

## ✍️ Типографика

### Шрифты

```css
/* Основной шрифт - для текста и интерфейса */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap');

/* Акцентный шрифт - для заголовков */
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&display=swap');

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 16px;
  line-height: 1.6;
  color: var(--color-text-main);
}

h1, h2, h3 {
  font-family: 'Playfair Display', serif;
  font-weight: 600;
  line-height: 1.3;
}

h1 {
  font-size: 32px;
}

h2 {
  font-size: 24px;
}

h3 {
  font-size: 20px;
}
```

### Размеры текста

- **Заголовки**: 24-32px (Playfair Display)
- **Основной текст**: 16px (Inter)
- **Второстепенный текст**: 14px (Inter)
- **Мелкий текст**: 12px (Inter)

---

## 🔲 Компоненты

### Кнопки

#### Основная кнопка (Primary)

```css
.button-primary {
  background: var(--color-primary);
  color: var(--color-text-inverse);
  border-radius: 16px;
  padding: 14px 28px;
  font-size: 16px;
  font-weight: 500;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px var(--color-shadow);
}

.button-primary:hover {
  background: #D88A97;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px var(--color-shadow);
}

.button-primary:active {
  transform: translateY(0);
}
```

#### Вторичная кнопка (Secondary)

```css
.button-secondary {
  background: var(--color-bg-secondary);
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
  border-radius: 16px;
  padding: 12px 26px;
  font-size: 16px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.3s ease;
}

.button-secondary:hover {
  background: var(--color-primary);
  color: var(--color-text-inverse);
}
```

### Игровое поле

```css
.game-board {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  max-width: 320px;
  margin: 0 auto;
}

.game-cell {
  aspect-ratio: 1;
  background: var(--color-bg-secondary);
  border: 2px solid var(--color-border);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.game-cell:hover:not(.disabled) {
  border-color: var(--color-primary);
  transform: scale(1.05);
  box-shadow: 0 4px 8px var(--color-shadow);
}

.game-cell.disabled {
  cursor: not-allowed;
  opacity: 0.6;
}

.game-cell.x {
  color: var(--color-x);
}

.game-cell.o {
  color: var(--color-o);
}
```

### Карточки и контейнеры

```css
.card {
  background: var(--color-bg-secondary);
  border-radius: 20px;
  padding: 24px;
  box-shadow: 0 4px 16px var(--color-shadow);
}

.modal {
  background: var(--color-bg-secondary);
  border-radius: 24px;
  padding: 32px;
  max-width: 400px;
  margin: 0 auto;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}
```

---

## 🎭 Анимации

### Принципы

- **Плавность**: Все анимации должны быть плавными (ease, ease-in-out)
- **Длительность**: 200-300ms для интерактивных элементов, 400-600ms для переходов
- **Масштаб**: Небольшие трансформации (scale 1.05-1.1, translateY -2px)

### Примеры анимаций

```css
/* Появление ячейки */
@keyframes cellAppear {
  from {
    opacity: 0;
    transform: scale(0.8);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.game-cell.filled {
  animation: cellAppear 0.3s ease;
}

/* Пульсация промокода */
@keyframes pulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.05);
  }
}

.promo-code {
  animation: pulse 2s ease-in-out infinite;
}

/* Победа - подсветка линии */
@keyframes winLine {
  from {
    opacity: 0;
    stroke-dasharray: 0 100;
  }
  to {
    opacity: 1;
    stroke-dasharray: 100 0;
  }
}
```

---

## 📱 Адаптивность

### Breakpoints

```css
/* Mobile First подход */
/* Базовые стили для мобильных */

/* Планшеты */
@media (min-width: 768px) {
  .game-board {
    max-width: 400px;
  }
}

/* Десктоп (если нужен) */
@media (min-width: 1024px) {
  .game-board {
    max-width: 480px;
  }
}
```

### Безопасные зоны

- Учитывать статус-бар Telegram
- Отступы от краёв экрана: минимум 16px
- Контент не должен перекрываться системными элементами

---

## 🎨 UI Состояния

### Экран победы

- Фон: градиент или мягкий цвет success
- Промокод: крупный шрифт, выделение цветом accent
- Анимация: плавное появление, пульсация промокода
- Кнопка: "Сыграть ещё"

### Экран проигрыша

- Фон: мягкий цвет error или основной bg
- Текст: дружелюбное сообщение с эмодзи
- Кнопка: "Попробовать ещё раз"

### Экран ничьей

- Фон: нейтральный
- Текст: "Ничья 💫"
- Кнопка: "Сыграть ещё раз"

### Состояние загрузки

- Индикатор: мягкий spinner или skeleton
- Цвет: primary
- Анимация: плавная ротация или пульсация

---

## 🎯 Доступность

- Контрастность текста: минимум 4.5:1
- Фокус-состояния для интерактивных элементов
- Поддержка клавиатурной навигации
- Семантическая HTML разметка

---

## 📐 Отступы и размеры

### Spacing Scale

```css
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-xxl: 48px;
}
```

### Border Radius

```css
:root {
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --radius-full: 9999px;
}
```

---

## 🎪 Эмодзи и иконки

### Использование эмодзи

- 💕 - Приветствие, дружелюбие
- 🎉 - Победа, успех
- 💫 - Ничья, нейтральные состояния
- ❌ - Крестик (игрок)
- ⭕ - Нолик (бот)

### Иконки

- Использовать SVG для масштабируемости
- Цвет: наследовать от родителя или использовать переменные
- Размер: 24px для стандартных иконок

---

## ✅ Чеклист использования

При создании новых компонентов проверять:

- [ ] Используются цвета из палитры
- [ ] Шрифты соответствуют типографике
- [ ] Отступы из spacing scale
- [ ] Border radius из системы
- [ ] Анимации плавные и не резкие
- [ ] Адаптивность для мобильных
- [ ] Состояния hover/focus/active
- [ ] Контрастность текста достаточна

