# 🔧 Инструкция по исправлению проблем безопасности

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА: Токен бота в репозитории

Обнаружено, что токен Telegram бота находится в публичных файлах документации. Это **критическая проблема безопасности**.

---

## 🚨 Немедленные действия

### Шаг 1: Отзовите текущий токен

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите бота `@TicTacToe_ru_bot`
4. Выберите "API Token"
5. Выберите "Revoke current token"
6. Сохраните новый токен в безопасном месте (пока не используйте в коде)

### Шаг 2: Удалите токен из истории Git

⚠️ **ВАЖНО**: Если репозиторий уже публичный на GitHub, выполните следующие действия:

#### Вариант A: Если репозиторий только что создан (нет важных коммитов)

```bash
# Перейти в директорию проекта
cd /Users/nikolai/Desktop/HW

# Проверить статус
git status

# Если токен только в последнем коммите, можно сделать:
git reset --soft HEAD~1
# Отредактировать файлы, удалив токены
git add .
git commit -m "Remove sensitive data"
git push --force origin main
```

#### Вариант B: Если уже есть история коммитов

Используйте `git-filter-repo` для очистки истории:

```bash
# Установить git-filter-repo (если нет)
pip install git-filter-repo

# Удалить токен из всей истории
git filter-repo --replace-text <(echo '8511369237:AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o==>REMOVED_TOKEN')

# Или удалить конкретные файлы из истории
git filter-repo --invert-paths --path примерный-запрос.md
git filter-repo --invert-paths --path ПРОЕКТ-ЗАПРОС.md

# Принудительно обновить удалённый репозиторий
git push --force origin main
```

⚠️ **ВНИМАНИЕ**: `--force` перезапишет историю. Убедитесь, что все участники проекта синхронизированы!

#### Вариант C: Использовать GitHub API (рекомендуется для публичных репозиториев)

Если репозиторий уже на GitHub и публичный:

1. Откройте репозиторий на GitHub
2. Перейдите в Settings → General → Danger Zone
3. Используйте "Remove sensitive data" или обратитесь в поддержку GitHub
4. Или используйте [GitHub API для удаления файлов](https://docs.github.com/en/rest/repos/contents#delete-a-file)

### Шаг 3: Обновите файлы

Токены уже удалены из файлов:
- ✅ `примерный-запрос.md` - обновлён
- ✅ `ПРОЕКТ-ЗАПРОС.md` - обновлён

### Шаг 4: Создайте `.env` файл локально

```bash
# Скопировать пример
cp .env.example .env

# Отредактировать .env и вставить новый токен
# Используйте ваш редактор или:
nano .env
# или
code .env
```

В `.env` файле укажите:
```env
BOT_TOKEN=ваш_новый_токен_от_BotFather
BOT_USERNAME=TicTacToe_ru_bot
WEB_APP_URL=http://localhost:3000
NODE_ENV=development
```

### Шаг 5: Проверьте `.gitignore`

Убедитесь, что `.gitignore` содержит:
```gitignore
.env
.env.*
!.env.example
```

Проверьте:
```bash
git check-ignore -v .env
# Должно показать, что .env игнорируется
```

### Шаг 6: Проверьте, что токен не попадёт в коммит

```bash
# Проверить статус
git status

# Убедитесь, что .env не в списке изменённых файлов
# Если .env в списке - проверьте .gitignore

# Проверить, что токен не в staged файлах
git diff --cached | grep -i "token\|8511369237"
# Не должно быть результатов
```

---

## ✅ Проверка безопасности

### Перед каждым коммитом выполняйте:

```bash
# 1. Проверить, что .env не отслеживается
git ls-files | grep "\.env$"
# Не должно быть результатов

# 2. Поиск токенов в изменённых файлах
git diff | grep -i "8511369237\|AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o"
# Не должно быть результатов

# 3. Проверить staged файлы
git diff --cached | grep -i "token\|secret\|password"
# Не должно быть результатов
```

---

## 📋 Настройка GitHub Secrets (для CI/CD)

Если используете GitHub Actions, настройте секреты:

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте секреты:
   - `BOT_TOKEN` - новый токен бота
   - `FTP_HOST` - хост FTP (если используется)
   - `FTP_USERNAME` - имя пользователя FTP
   - `FTP_PASSWORD` - пароль FTP
   - `FTP_WEB_PATH` - путь для Web App на сервере

В workflow файлах используйте:
```yaml
env:
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
```

---

## 🔍 Дополнительная проверка

### Проверить историю Git на наличие токенов

```bash
# Поиск в истории коммитов
git log --all --full-history -p | grep -i "8511369237\|AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o"

# Если найдены результаты - нужно очистить историю (см. Шаг 2)
```

### Проверить все файлы в репозитории

```bash
# Поиск токенов во всех файлах (кроме node_modules и .git)
find . -type f -not -path "./node_modules/*" -not -path "./.git/*" -exec grep -l "8511369237\|AAGpJNwGv1F3wX3QMbOJfWPGSSLf9z1jV5o" {} \;

# Если найдены файлы - удалите токены из них
```

---

## 📝 Чеклист исправления

- [ ] Токен отозван в BotFather
- [ ] Новый токен получен и сохранён безопасно
- [ ] Токен удалён из всех файлов в репозитории
- [ ] История Git очищена от токена (если репозиторий публичный)
- [ ] `.env` файл создан локально с новым токеном
- [ ] `.env` добавлен в `.gitignore`
- [ ] `.env.example` создан без реальных значений
- [ ] Проверено, что `.env` не отслеживается Git
- [ ] GitHub Secrets настроены (если используется CI/CD)
- [ ] Все коммиты проверены на отсутствие токенов

---

## 🛡️ Предотвращение проблем в будущем

1. **Всегда используйте `.env` файлы** для секретов
2. **Проверяйте `.gitignore`** перед коммитом
3. **Используйте `git-secrets`** или pre-commit hooks
4. **Регулярно проверяйте** историю на наличие секретов
5. **Используйте GitHub Secrets** для CI/CD

---

## 📚 Дополнительная информация

См. файл `SECURITY.md` для полного руководства по безопасности.

---

**После выполнения всех шагов ваш проект будет безопасным! 🔒**

