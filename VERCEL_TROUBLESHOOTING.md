# 🔧 Решение проблемы 403 на Vercel

## Проблема: 403 Forbidden для setup-webhook.php

Если вы получаете ошибку 403 при попытке открыть `/setup/setup-webhook.php`, это может быть связано с конфигурацией Vercel.

## ✅ Решение

### Вариант 1: Использовать прямой путь к API

Попробуйте открыть:
```
https://tic-tac-toe-virid-two.vercel.app/api/setup-webhook.php
```

Файл теперь также доступен по этому пути.

### Вариант 2: Проверить конфигурацию

1. Убедитесь, что в `vercel.json` правильно настроены маршруты
2. Проверьте, что переменная `BOT_TOKEN` установлена в Vercel Dashboard
3. Перезапустите деплой в Vercel Dashboard

### Вариант 3: Проверить логи

1. Откройте Vercel Dashboard → Functions → Logs
2. Найдите ошибки, связанные с `setup-webhook.php`
3. Проверьте, что PHP runtime правильно установлен

## 🔍 Диагностика

### Проверка доступности файлов

Попробуйте открыть:
- `https://tic-tac-toe-virid-two.vercel.app/webhook.php` - должен вернуть JSON или ошибку (но не 403)
- `https://tic-tac-toe-virid-two.vercel.app/api/game.php` - должен вернуть ошибку метода (но не 403)
- `https://tic-tac-toe-virid-two.vercel.app/api/setup-webhook.php` - должен открыться

### Если все возвращает 403

1. Проверьте, что проект правильно подключен к Vercel
2. Убедитесь, что файлы из `dist/` и `api/` загружены
3. Проверьте настройки проекта в Vercel Dashboard → Settings → General

## 📝 Альтернативное решение

Если проблема сохраняется, можно использовать альтернативный способ настройки webhook через curl:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://tic-tac-toe-virid-two.vercel.app/webhook.php"}'
```

Замените `<YOUR_BOT_TOKEN>` на ваш токен бота.

